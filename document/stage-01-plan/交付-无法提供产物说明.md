# 最终交付 — 无法提供产物说明

> 生成日期: 2026-08-07  
> 项目: 毛线球宠物医院 SaaS (maoxianqiu)  
> 阶段: Stage 01 集成冲刺与验证

---

## 已提供

| 产物 | 状态 | 详情 |
|------|------|------|
| 最新 commit SHA | 已提供 | `f4dad3e8` |
| 完整源码 ZIP | 已提供 | `D:\2026-out-going-standard\maoxianqiu-net-f4dad3e8.zip` |

---

## 无法提供产物及原因

### 1. Vercel Preview URL

**状态:** 无法提供  
**原因:** 缺少 Vercel 项目令牌和部署权限。

**所需前置条件:**
- Vercel 账号中创建 `maoxianqiu-staging` 项目
- 配置环境变量: `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`(指向 staging Supabase)
- 配置 Hono API 所需的环境变量: `SUPABASE_SERVICE_ROLE_KEY`, `R2_*`
- 运行 `pnpm --filter @fantastic-admin/maoxianqiu build` 后部署

**操作指引:**

```bash
# 安装 Vercel CLI
pnpm add -g vercel

# 关联项目
vercel link

# 设置环境变量
vercel env add VITE_SUPABASE_URL
vercel env add VITE_SUPABASE_ANON_KEY

# 部署 Preview
vercel --prod
```

---

### 2. Supabase Staging 信息

**状态:** 无法提供  
**原因:** 当前 `.env.development` 指向生产 Supabase 项目 (`bxhvtbhwuktrpxxygikj`)。
需要在 Supabase Dashboard 创建独立的 staging 项目。

**所需操作:**

1. 访问 https://supabase.com/dashboard
2. 创建新项目，建议命名 `maoxianqiu-staging`
3. 获取项目 ref 和 service_role key
4. 在项目根目录执行:
   ```bash
   pnpm db:link --project-ref <staging-ref>
   pnpm db:reset   # 从 migration 01 连续应用到 24 + 执行 seed
   ```
5. 记录并回传:
   - Supabase URL
   - Anon Key
   - Service Role Key
   - 项目 ref

---

### 3. Migration 01～24 执行日志

**状态:** 无法提供  
**原因:** 需要可用的 Supabase staging 数据库才能执行。

验证通过后获取方式:

```bash
# 完整日志
pnpm db:reset 2>&1 | tee migration.log

# schema diff (与空库对比)
pg_dump "$STAGING_DATABASE_URL" --schema-only > staging-schema.sql
```

当前 24 个 migration 文件和 RPC 签名清单静态分析已完成，所有函数通过 `create or replace` 幂等定义，可安全连续执行。

---

### 4. 全部 RLS 测试日志

**状态:** 无法提供  
**原因:** 10 个 RLS 测试脚本位于 `supabase/tests/`，需要 staging 数据库执行。

测试文件清单:

| 文件 | 测试范围 |
|------|---------|
| `rls_tenant_store.sql` | 两租户两门店隔离测试 |
| `rls_files.sql` | 文件/附件 RLS |
| `rls_crm.sql` | 客户/宠物 RLS |
| `rls_catalog.sql` | 目录/价格 RLS |
| `rls_clinical.sql` | 诊疗 RLS |
| `rls_billing.sql` | 收费 RLS |
| `rls_inventory.sql` | 库存 RLS |
| `rls_diagnostics.sql` | 疫苗/检验 RLS |
| `rls_inpatient.sql` | 住院 RLS |
| `rls_operations.sql` | 运营 RLS |

执行方式(staging 就绪后):

```bash
for f in supabase/tests/rls_*.sql; do
  echo "=== Running $f ==="
  psql "$STAGING_DATABASE_URL" -f "$f"
done
```

---

### 5. Playwright 报告

**状态:** 无法提供  
**原因:** E2E 测试需要 Chromium 浏览器 + staging Supabase 凭据(`E2E_USERNAME`/`E2E_PASSWORD`)。

获取方式:

```bash
# 设置凭据
$env:E2E_USERNAME = "staging-admin@example.com"
$env:E2E_PASSWORD = "your-password"

# 运行测试
pnpm --dir e2e exec playwright test

# 打开报告
pnpm --dir e2e exec playwright show-report
```

报告产物: `e2e/playwright-report/` (HTML) + `e2e/test-results/junit.xml` (JUnit)

CI 中自动执行: `.github/workflows/e2e.yml` 配置了自动安装 Chromium + 执行测试 + 上传 HTML/JUnit/截图/trace

---

### 6. 三条核心业务闭环截图或录屏

**状态:** 无法提供  
**原因:** 需要 staging 环境中有完整的测试数据(2 tenant / 3 store / 多角色用户 / 库存 / 目录等)。

数据准备完毕后，三个闭环的预期验证路径:

**闭环 A:**
```
客户 → 宠物 → 预约 → 候诊 → 就诊 → 病历签署 → 处方 → 收费 → 支付 → 发药 → 库存扣减
```
对应 E2E 测试: `e2e/tests/closed-loop-a.spec.ts`

**闭环 B:**
```
采购入库 → 库存余额 → 盘点 → 调拨 → 库存流水
```
涉及模块: `inventory/receipt` → `inventory/dashboard` → `inventory/count` → `inventory/transfer`

**闭环 C:**
```
住院 → 护理任务 → 换房 → 自动计费 → 出院结算
```
涉及模块: `inpatient/admission` → `inpatient/nursing` → `inpatient/handover` → `billing/cashier`

---

## 静态验证已完成项

| 项目 | 状态 |
|------|------|
| 24 个 migration 文件结构完整性 | 通过 |
| 62 个 RPC 签名清单(最终版本) | 通过 |
| SECURITY DEFINER search_path 统一为 `public` | 通过 |
| grant execute 统一 `revoke → grant to authenticated` | 通过 |
| RLS 递归风险排查 | 无递归 |
| audit_logs 写入点确认 | 已确认(clinical/diagnostics) |
| dispense_inventory 最终版本 | 00024(含 reserve) |
| scan_reminders 最终版本 | 00023(完整) |
| send_delivery 最终版本 | 00023(完整) |
| generate_report_snapshot 最终版本 | 00023(完整) |
