# 毛线球 SaaS — 六 Agent 并发开发完成后源码审计报告

> **审计版本**：Post-Parallel Code Audit v1  
> **审计对象**：六个并发 Agent + Agent-07 最终集成后的源码  
> **基线阶段**：Stage-03 / S3.1  
> **审计代码包**：`cc93addc-bb95-486c-8d07-40bcf2874191.zip`  
> **审计范围**：源码、Migration、RLS、RPC ACL、API、权限、跨域状态机、六个新模块、核心 UI/后端一致性  
> **明确排除**：**E2E 本轮不参与评审**。E2E 仍作为独立 Runtime/Pilot Gate 工作流执行。
>
> 本次上传包属于增量交付包。审计时已将增量完整应用到上一版源码基线上，**130 个变更文件均可干净应用**，因此本报告基于还原后的完整最终源码进行。

---

# 1. 结论先行

六 Agent 并发开发方案本身是成功的。

六个新需求都不是“只写了文档”或“只放了占位页面”，而是已经进入真实源码：

- Agent-01：平台租户管理 / 门店详情 / Context
- Agent-02：会员等级 / 积分 / 折扣规则 / Billing 接入
- Agent-03：影像工作流
- Agent-04：Customer 360 / 回访任务
- Agent-05：供应商 / 采购订单 / 库存过账
- Agent-06：寄养
- Agent-07：Migration 90/91、跨域集成与最终报告

因此：

> **并发开发架构是有效的，模块本身不需要整体推倒重做。**

但是，本次源码审计不接受当前 Agent-07 文档中“关键安全边界全部通过”的结论。

当前真实状态更准确地应该标记为：

```text
6 个业务模块：
code_complete / integration_present

系统整体：
security_hardening_required
integration_fix_required
runtime_verification_pending
```

而不是：

```text
pilot_ready
```

---

# 2. 本轮最重要的新发现

当前最大的风险并不是：

```text
页面没做
API 没写
Migration 缺失
```

而是：

> **早期 Stage-03 已经做过的安全收口，被六 Agent 后续 Migration 中的 `CREATE OR REPLACE` / `GRANT authenticated` 部分重新打开。**

另外：

> **Agent-01 为了增加“租户停用”能力，重新覆盖了数据库权限 Helper，但覆盖后的版本比之前 S3.0 Hardened 版本弱。**

因此出现了一个典型的并发开发集成问题：

```text
单个 Agent 看自己的代码：正确
↓
所有 Migration 顺序执行后：
早期安全约束被后期功能 Migration 覆盖
↓
Agent-07 静态脚本仍显示 PASS
```

这也是本轮最需要处理的地方。

---

# 3. 最终评级

| Agent | 新模块完成度 | 架构质量 | 当前评级 | 处理建议 |
|---|---:|---:|---|---|
| Agent-01 IAM/平台 | 高 | 中偏低 | 🔴 需回炉核心部分 | 修权限/Context，不重做 UI |
| Agent-02 财务/会员 | 高 | 中高 | 🟠 补强后可保留 | 修 ACL、规则真实性、Tenant 校验 |
| Agent-03 医疗/影像 | 高 | 中 | 🟠/🔴 医疗状态需补强 | 保留影像模块，重做关键状态边界 |
| Agent-04 CRM/回访 | 高 | 中高 | 🟠 | 修 Context、Entity 校验、RLS 写路径 |
| Agent-05 采购 | 高 | **六组最好** | 🟡/🟢 | 修仓库/门店关系与部分收货 |
| Agent-06 寄养 | 高 | 中高 | 🟠 | 修 Direct Write、幂等、预订冲突 |
| Agent-07 集成 | 合并成功 | 安全复审不足 | 🔴 需第二次 Integrator Review | 重点重做最终 ACL/权限审计 |

---

# 4. Agent-07 的 RPC 安全结论存在“假绿”

这是本轮 **最高优先级 P0**。

Agent-07 新增：

```text
api/scripts/check-rpc-manifest.ts
```

报告称：

```text
manifest 全部 RPC 已纳入
revoke public/anon/authenticated
grant service_role
```

但是脚本实际并没有验证数据库最终 ACL。

---

# 5. `check:rpc-manifest` 的真实逻辑

规则 2 的实现：

```ts
const re = /'([a-z_][a-z0-9_]*)'/g
```

它把：

```text
supabase/migrations/*.sql
```

中的**所有单引号字符串**收集起来。

只要：

```text
'create_invoice'
```

这个名字曾经出现在任意 Migration：

- ARRAY
- COMMENT
- SQL 字符串
- REVOKE
- GRANT
- 普通业务内容

都可以满足：

```text
“已出现在 revoke 清单”
```

所以：

```text
check:rpc-manifest = PASS
```

并不等于：

```text
最终 authenticated 无 EXECUTE
```

---

# 6. P0-01：后续 Migration 重新给 authenticated 开放敏感 RPC

## `20260809000055_effective_setting_integration.sql`

重新：

```sql
GRANT EXECUTE ON FUNCTION public.create_invoice(...) TO authenticated;
GRANT EXECUTE ON FUNCTION public.confirm_invoice(...) TO authenticated;
```

---

## `20260809000056_approval_self_review.sql`

重新：

```sql
GRANT EXECUTE ON FUNCTION public.approve_discount(...) TO authenticated;
GRANT EXECUTE ON FUNCTION public.review_record_amendment(...) TO authenticated;
```

---

## `20260810000057_membership_billing_integration.sql`

重新：

```sql
GRANT EXECUTE ON FUNCTION public.get_effective_membership_discount(...) TO authenticated;

GRANT EXECUTE ON FUNCTION public.preview_membership_discount(...) TO authenticated;

GRANT EXECUTE ON FUNCTION public.create_invoice(..., boolean) TO authenticated;
```

---

## `20260810000073_boarding_service_charges.sql`

仍有：

```sql
GRANT EXECUTE ON FUNCTION public.boarding_prepare_checkout(...) TO authenticated;
```

Migration 91 收紧了：

```text
boarding_checkout
```

但没有同步收紧：

```text
boarding_prepare_checkout
```

---

# 7. Migration 90 没有覆盖全部回归 RPC

`20260810000090_rpc_manifest_reconciliation.sql` 只收紧：

```text
publish_imaging_report

create_purchase_order
update_purchase_order_draft
submit_purchase_order
approve_purchase_order
cancel_purchase_order
receive_purchase_order
post_purchase_order

preview_membership_discount
```

没有重新收紧：

```text
create_invoice
confirm_invoice
approve_discount
review_record_amendment
boarding_prepare_checkout
```

以及：

```text
get_effective_membership_discount
```

后者虽然不是 Hono route manifest 中的显式 RPC，但本身是：

```text
SECURITY DEFINER
```

且调用时完全信任：

```text
tenant_id
store_id
customer_id
```

因此 authenticated 直接调用可能跨业务边界读取会员折扣信息。

---

# 8. 为什么这是 Release Blocker

Stage-03 的安全架构已经确定：

```text
Browser
↓
Hono
↓
requireScopedPermission
↓
service role RPC
↓
DB
```

敏感 Command 不应该：

```text
Browser
↓
Supabase RPC
```

直接调用。

如果：

```text
create_invoice
approve_discount
review_record_amendment
```

仍可 authenticated 调用，

用户可能绕过：

- Hono Scope；
- Hono 参数校验；
- Hono Audit；
- 业务 Operator 身份；
- 部分状态机。

这是 P0。

---

# 9. 特别危险：`approve_discount` 的 Operator 参数可伪造

即使 Migration 56 已增加：

```text
requested_by != approved_by
```

但 RPC 的：

```text
p_approved_by
```

仍由调用者传入。

如果浏览器可以直接 RPC：

```text
当前用户 A
↓
传一个 Employee B 的 UUID
↓
requested_by != B
```

数据库仅靠参数比较并不能证明：

```text
真正操作人就是 B
```

Hono 层本来负责绑定真实操作者。

因此：

> **禁止自审只有和 Service-Role-Only Command 边界组合起来才真正成立。**

---

# 10. 修复建议：Migration 92

建议 Agent-07 / Security Fix Agent 新建：

```text
20260810000092_rpc_acl_final_lockdown.sql
```

在**所有当前 Feature Migration 后**执行。

用动态 OID/Signature 方式统一：

```sql
REVOKE ALL ... FROM public;
REVOKE ALL ... FROM anon;
REVOKE ALL ... FROM authenticated;
GRANT EXECUTE ... TO service_role;
```

目标名单至少以：

```text
SERVICE_ROLE_ONLY_RPC
```

为唯一事实来源。

另外补：

```text
boarding_prepare_checkout
create_invoice 所有 overload
confirm_invoice
approve_discount
review_record_amendment
```

---

# 11. RPC CI 也必须升级

静态搜索函数名字不够。

正确验证方案：

```text
创建 CI Postgres / Supabase DB
↓
应用全部 Migration
↓
查询 pg_proc / has_function_privilege()
```

例如：

```sql
select
  p.oid::regprocedure,
  has_function_privilege(
    'authenticated',
    p.oid,
    'EXECUTE'
  )
from pg_proc p
join pg_namespace n
  on n.oid = p.pronamespace
where n.nspname='public';
```

对于 Manifest 中函数：

```text
authenticated_execute
```

必须全部：

```text
false
```

这才能叫真正 RPC ACL Gate。

---

# 12. P0-02：Agent-01 覆盖了已经 Hardened 的 `has_permission()`

早期：

```text
20260807000026_scoped_permission_hardening.sql
```

已经建立正确版本：

```text
role.scope
+
employee_role_assignments
+
role_permissions
+
legacy roles.permissions
```

并严格区分：

```text
tenant context
store context
store role
tenant role
system role
```

---

# 13. Agent-01 Migration 54 把 Helper 降级了

`20260810000054_platform_tenant_mgmt.sql`

重新：

```sql
CREATE OR REPLACE FUNCTION public.has_permission(...)
```

但新版本只主要判断：

```text
r.permissions[]
```

丢失：

```text
role_permissions
```

关系表。

也丢失原先严格：

```text
role.scope
```

语义。

现在：

```sql
p_store_id is null
or era.store_id = p_store_id
or era.store_id is null
```

不足以表达：

```text
store role 不能提升为 tenant 权限
```

---

# 14. `can_access_store()` 也产生权限模型不一致

Agent-01 新版本：

```text
只有 employee_store_assignments
```

才能访问门店。

但是 Hono：

```text
tenant/system scope role
```

应该可以访问 Tenant 下所有 Store。

因此可能出现：

```text
Hono：
你有 Tenant Owner 权限

DB RLS：
你没 employee_store_assignment
→ 拒绝
```

即：

```text
Hono 权限模型
≠
RLS 权限模型
```

---

# 15. 修复原则

不要在 Migration 54 上修改历史。

新建：

```text
20260810000093_permission_helper_restore.sql
```

以 Hardened 26 为基础：

```text
恢复 scope 语义
恢复 role_permissions
保留 legacy fallback
```

然后只叠加：

```text
tenant status
```

检查。

---

# 16. P0-03：Trial Tenant 当前实际不可稳定使用

Tenant 状态支持：

```text
active
trial
suspended
```

但三个层面行为不一致。

---

## Hono

`resolveScopedAccess`：

```text
只阻止 suspended
```

所以：

```text
trial
```

允许。

---

## Agent-01 SQL Helper

却要求：

```sql
tenant.status = 'active'
```

所以：

```text
trial
```

RLS 拒绝。

---

## `/api/user/context`

又过滤：

```ts
status === 'active'
```

所以：

```text
trial
```

不会进入 Context。

---

# 17. `resume_tenant()` 也会把 Trial 直接变 Active

当前逻辑：

```text
只要 status != active
↓
resume
↓
active
```

因此：

```text
trial
```

也可以被 “恢复” 成：

```text
active
```

不符合状态语义。

---

# 18. Trial 正确模型

统一定义：

```text
active
→ 正常业务

trial + trial_ends_at 未过期
→ 正常业务

trial 已过期
→ 只读/限制/阻断（根据产品策略）

suspended
→ 禁止业务 Command
```

三个层面必须一致：

```text
Hono
RLS
Context
```

---

# 19. P0-04：前端 Permission 仍是“所有门店权限并集”

`api/lib/me-context.ts`：

```text
globalPerms = new Set()
```

最终：

```text
把所有 Tenant / Store 的 permission 合并
```

顶层返回：

```text
permissions: [...globalPerms]
```

前端：

```text
getPermissions()
↓
直接使用这个 permissions
```

---

# 20. 问题场景

用户：

```text
Store A：
doctor

Store B：
cashier
```

选择：

```text
Store B
```

前端仍可能拥有：

```text
doctor 权限菜单
+
cashier 权限菜单
```

因为它们来自 Global Union。

后端最终可能 403，但：

```text
前端权限展示
≠
当前 Context 权限
```

---

# 21. 正确方案

Context 应同时返回：

```text
platformPermissions
tenantPermissions
storePermissions
```

当前前端只消费：

```text
effectivePermissions(currentTenant,currentStore)
```

切 Store：

```text
立即重新计算 permissions
```

必要时重新生成：

```text
permission-based route/menu/action
```

---

# 22. P0-05：顶部切 Tenant/Store 没有进入 API Request Context

这是当前多 Tenant 产品最核心的问题之一。

服务端已经支持：

```text
x-tenant-id
x-store-id
```

但是 Axios Interceptor 目前只发送：

```text
Authorization
Token
Accept-Language
```

没有：

```text
X-Tenant-Id
X-Store-Id
```

---

# 23. 后端大量路由仍用：

```ts
getContext(c).memberships[0]?.tenant_id
```

作为默认 Tenant。

这意味着：

```text
顶部选择 Tenant B
```

不一定会影响服务端当前 Tenant。

多 Tenant 用户可能：

```text
页面显示：Tenant B

后端默认：
memberships[0] = Tenant A
```

---

# 24. Context 修复

Axios：

```text
X-Tenant-Id = currentTenantId
X-Store-Id = currentStoreId
```

但注意：

> Header 只是工作上下文，不是权限依据。

服务器必须继续：

```text
requireScopedPermission
```

重新验证。

然后逐步清理：

```text
memberships[0]
```

只保留为旧接口兼容 fallback。

---

# 25. P0-06：切门店后并不是所有页面都会重新加载

已经新增：

```text
useStoreScopedPage()
```

这是正确方向。

但当前只在部分 View 使用。

仍有多类页面未统一接入，例如：

- Billing 部分页面；
- Customer Detail；
- System User；
- System Settings；
- Inpatient Dashboard；
- Nursing；
- Critical Values；
- Vaccination；
- Reminder；
- Lab Samples；
- Print；
- Membership；
- Import；
- Approval。

---

# 26. 用户可能看到：

```text
顶部：
城南店
```

但主体仍是：

```text
总店旧数据
```

这属于业务上下文错误，不是普通 UI 问题。

---

# 27. `useStoreScopedPage()` 的 Run ID 也不够

当前：

```text
await load()
↓
再判断 runId
```

但是：

```text
load()
```

内部通常已经把响应写入：

```text
page state
```

旧请求即使最后发现：

```text
runId 已过期
```

也已经污染界面。

---

# 28. 正确处理

二选一：

### A

```text
AbortController
```

切 Context 时取消旧 Request。

### B

Loader：

```text
return data
```

Composable：

```text
确认 token 仍然有效
↓
再 Commit State
```

不要：

```text
load 内部先写状态
```

---

# 29. P0-07：Dirty Guard 仍不是真正的离开保护

`useUnsavedChangesGuard.ts`：

当前主要实现：

```text
全局 dirty Set
+
confirmLeave
```

但没有真正注册：

```text
onBeforeRouteLeave
beforeunload
```

也没有完整：

```text
onUnmounted
onDeactivated
```

清理。

---

# 30. 当前仅少量页面使用

静态检查主要是：

```text
Cashier
Clinical Workbench
Lab
```

而仍需要覆盖：

```text
Encounter Detail
Inventory Receipt
Inventory Count
Inventory Transfer
Inpatient Admission
Settlement
Imaging Report
Purchase Draft
Boarding
Settings 复杂编辑
```

---

# 31. P0-08：Password Recovery 仍未完成上一轮要求

虽然：

```text
/auth/reset-password
```

页面已经存在。

但仍有三个问题。

---

## 8.1 Router Guard

未登录时主要只允许：

```text
login
```

Recovery 页面没有被明确纳入：

```text
PUBLIC_AUTH_ROUTES
```

存在：

```text
Recovery Session 还没初始化
↓
Router Guard 先跳 Login
```

的 Race。

---

## 8.2 普通 Session 被当 Recovery

Reset 页面：

```text
如果 data.session 存在
→ recovered=true
```

这会把：

```text
普通已登录 Session
```

也当作：

```text
Password Recovery
```

---

## 8.3 Reset Email 没有明确 Redirect

仍：

```ts
resetPasswordForEmail(account)
```

没有显式：

```text
redirectTo:
origin + /auth/reset-password
```

正式部署后依赖 Supabase Dashboard 默认配置。

---

# 32. Agent-02 总体评价

Agent-02 是：

```text
“功能完成度高，真实产品价值明显”
```

的一组。

值得保留：

- System Settings nullable unique 已正确修；
- Settings Registry 已出现；
- Billing 折扣审批阈值已经真正接入 DB；
- Cashier 已区分 Tendered / Applied；
- Payment Context 已接入；
- 会员中心完整度明显提高；
- 会员价格快照方向正确；
- Approval Center 进一步完善。

但还有几个重要问题。

---

# 33. P0-09：Settings 仍有“保存成功但业务不读取”的配置

目前 UI / Registry 暴露：

```text
discount approval threshold
refund approval threshold
inventory adjustment threshold
transfer approval enabled
near expiry reminder days
```

其中：

```text
discount approval threshold
```

已经真实接 Billing。

但其它几个目前没有找到真实业务 Command/RPC 消费。

---

# 34. 处理原则

只有两个选择：

### 真接入

例如：

```text
Refund Command
→ 读取 refund threshold
→ 超阈值产生 Approval
```

### 暂不实施

则 UI：

```text
隐藏
```

或：

```text
Disabled
尚未启用
```

不能：

```text
保存成功
```

但实际上不起作用。

---

# 35. P0-10：Cashier 会员折扣正确性仍依赖前端 Preview

Cashier 现在先请求：

```text
Membership Discount Preview
```

再决定：

```text
applyMembershipDiscount
```

当前逻辑类似：

```text
customerId 存在
AND
memberDiscount > 0
→ true
```

---

# 36. 问题

如果：

```text
Preview 请求慢
Preview 失败
Watch 没触发
Cart 原地 mutation 没被监听
```

则：

```text
memberDiscount = 0
```

最后会向 Server 传：

```text
applyMembershipDiscount=false
```

即：

> **真正价格正确性被 UI Preview 状态影响。**

---

# 37. 正确模式

只要：

```text
已选择 customer
```

服务器就应该：

```text
自行判断有没有有效会员
```

前端：

```text
applyMembershipDiscount=true
```

Preview 仅用于：

```text
展示预计优惠
```

而不是决定真实收费规则。

---

# 38. Cashier 仍有 10% UI 硬编码

服务端阈值已经可配置。

但前端仍有：

```text
discount > 10
```

或：

```text
超 10% 需审批
```

类提示。

应改：

```text
从 Effective Settings 读取
```

仅用于 UI 提示。

最终判断仍由后端。

---

# 39. Payment Context 的“全部停用”存在 fallback 问题

如果管理员把所有方式：

```text
is_active=false
```

Cashier 当前某些 fallback 路径可能重新回到：

```text
静态默认支付方式
```

这意味着：

```text
管理员禁用了现金
↓
Cashier 又把现金显示出来
```

---

# 40. 正确行为

区分：

```text
API Load Failed
```

和：

```text
Load Success + 0 Active Method
```

后者应显示：

```text
当前门店没有可用支付方式，请联系管理员。
```

并禁止结算。

---

# 41. P0-11：会员相关跨 Tenant 引用缺少服务端完整校验

Hono 使用 Service Role 写：

```text
customer_memberships
membership_discount_rules
```

部分 Endpoint 允许传：

```text
tierId
storeId
catalogItemId
```

但是没有全部验证：

```text
tier 同 tenant
store 同 tenant
catalog item 同 tenant
customer 同 tenant
```

---

# 42. 为什么 FK 不够

普通 FK 只能证明：

```text
这个 UUID 存在
```

不能证明：

```text
它属于当前 Tenant。
```

Service Role 又绕过 RLS。

因此必须由 Hono/RPC显式验证。

---

# 43. Agent-03：影像模块是真实完整模块，但安全与状态需要补强

已经落地：

```text
影像申请
排程
执行
附件
报告
审核
发布
```

UI/数据模型/权限都不是空壳。

因此不需要重写。

但当前存在医疗级 P0。

---

# 44. P0-12：Imaging / Followup / Boarding 新表重新开放 Browser Direct Write

例如：

## Imaging

Migration 59：

```text
imaging_orders
  INSERT authenticated
  UPDATE authenticated

imaging_reports
  INSERT authenticated
  UPDATE authenticated
```

---

## Followup

Migration 62：

```text
INSERT authenticated
UPDATE authenticated
DELETE authenticated
```

---

## Boarding

Migration 71–73：

```text
boarding_stays
  INSERT / UPDATE / DELETE authenticated

boarding_daily_records
  INSERT / UPDATE / DELETE authenticated

boarding_service_charges
  INSERT / DELETE authenticated
```

---

# 45. 这和当前项目架构冲突

当前 Stage-03 已明确：

敏感业务：

```text
Browser
↓
Hono Command
↓
RPC/Transaction
↓
Audit
```

而这些新 RLS Policy 会形成潜在：

```text
Browser
↓
Supabase Table DML
↓
直接绕过 Command
```

---

# 46. 实际绕过例子

### Imaging

可绕过 Hono 状态机直接：

```text
UPDATE status
UPDATE report
```

---

### Followup

拥有 `followup.manage` 时可以直接：

```text
UPDATE status='completed'
```

绕过：

- complete permission；
- result 必填；
- audit。

---

### Boarding

可以直接：

```text
改 cage_id
改 status
改 total_charge
```

绕过：

- 笼位锁；
- Billing；
- Audit；
- Checkout 状态机。

---

# 47. 修复

对于这些新领域：

```text
SELECT
```

可以保留 RLS。

敏感写：

```text
INSERT
UPDATE
DELETE
```

应收回 Browser 权限。

通过：

```text
Hono service role
+
RPC
```

执行。

建议 Migration 94：

```text
20260810000094_new_domain_command_boundary.sql
```

统一删除/收紧写 Policy。

---

# 48. P0-13：Imaging 创建缺少实体关系验证

`POST /diagnostics/imaging/orders`：

虽然验证：

```text
tenant/store permission
```

但 Service Role Insert 前没有完整证明：

```text
customerId 属于 tenant
petId 属于 customer
petId 属于 tenant
encounterId 属于 pet
encounterId 属于当前 store/tenant
catalogItemId 属于 tenant
```

---

# 49. 正确方式

建立：

```text
validateImagingOrderContext()
```

或单个 Transaction/RPC。

至少验证：

```text
Encounter
↓
pet
↓
customer
↓
tenant/store
```

形成一致链。

---

# 50. P0-14：Imaging 状态转换存在并发竞争

当前很多 Command：

```text
先 SELECT status
↓
代码判断
↓
UPDATE id
```

UPDATE 没有：

```text
AND status = expected
```

也没有：

```text
FOR UPDATE
```

两个并发请求都可能先读到相同状态，然后都成功更新。

医疗状态建议：

```text
RPC + SELECT FOR UPDATE
```

或：

```text
conditional UPDATE status=expected
```

并检查：

```text
affected row = 1
```

---

# 51. Imaging Report 创建时机也需要收紧

当前 Report Create 对：

```text
requested
scheduled
in_progress
```

限制不够严格。

产品状态是：

```text
执行完成
↓
报告
```

所以至少：

```text
performed
```

之后才能正式建立报告。

---

# 52. 已发布报告创建 Revision 时 Order 状态不完整

如果：

```text
order = published
```

又创建：

```text
V2 draft
```

Order 仍可能维持：

```text
published
```

用户从 Stage Tab 看不到：

```text
存在待审 Revision
```

建议：

```text
latestReportStatus
```

进入 workflow DTO。

不要只靠：

```text
imaging_orders.status
```

表达所有报告生命周期。

---

# 53. P0-15：Lab Workflow 仍没有真正解决“审核 vs 发布”语义

Agent-03 已经新增：

```text
deriveLabWorkflow()
```

方向是正确的。

但是：

```text
awaiting_review
```

当前仍可能同时：

```text
canPublish = true
canReview = true
```

UI 因而可能同时出现：

```text
发布结果
审核通过
驳回
```

---

# 54. 这和成熟检验流程不一致

应明确：

```text
录入结果
↓
提交审核
↓
审核通过/驳回
↓
发布
```

不能：

```text
待审核
```

阶段同时把：

```text
发布
```

作为主动作。

---

# 55. 如果当前数据库 RPC 语义实际上是：

```text
publish_lab_results
= 保存结果并进入待审核
```

则应该：

```text
改 UI / API 命名
```

例如：

```text
submit_lab_results
```

而不是让业务用户看到：

```text
发布
```

却其实只是提交审核。

---

# 56. Agent-04：Customer 360 与 Followup 方向正确

值得保留：

- Customer Detail 真实就诊历史已补；
- Customer 360 明显比上一版完整；
- Followup Task 独立 Domain 已建立；
- 状态机 pending/in_progress/completed/cancelled 基础合理；
- 客户详情已能承接回访。

主要问题是：

```text
Context / Entity Validation / Timezone / Direct Write
```

---

# 57. P0-16：Followup 仍使用 `memberships[0]`

部分 Followup API：

```text
getContext(c).memberships[0]?.tenant_id
```

因此也受：

```text
当前 Tenant Selector
≠
服务端 Tenant
```

的问题影响。

必须一起随 P0-05 修。

---

# 58. Followup 创建缺少实体关系验证

需要验证：

```text
customer
pet
assignee
source_type/source_id
```

都属于：

```text
同 Tenant
同业务关系
```

尤其：

```text
pet.customer_id = customer_id
```

不能只靠 UUID 存在。

---

# 59. Followup 的“今天”不是 Tenant/Store 时区

服务器目前用：

```text
new Date()
setHours(...)
```

基于 Server Timezone。

但产品已有：

```text
Tenant Timezone
Store Override
```

回访：

```text
今天
逾期
```

必须按业务时区计算。

---

# 60. Clinical Follow-up Hook 还有硬编码 `+08:00`

生成：

```text
09:00 +08:00
```

这和可配置时区冲突。

必须：

```text
effectiveTimezone
```

计算。

---

# 61. Agent-05：采购是本轮架构完成质量最高的模块

值得肯定的地方：

- 新建 Supplier；
- Purchase Order Header/Item；
- 状态机；
- Hono/RPC Command；
- Service-role-only 方向；
- `SELECT FOR UPDATE`；
- Idempotency；
- 采购过账复用：
  `post_goods_receipt`
- 没有新写第二套库存余额算法。

这套方向应该作为其它新模块参考。

---

# 62. 但采购仍有两个 P0 业务完整性问题

## P0-17A：Warehouse 未限制属于当前 Store

`create_purchase_order`：

当前验证 Warehouse：

```text
id
tenant_id
is_active
```

但没有：

```text
warehouse.store_id = p_store_id
```

因此可能：

```text
PO Store = A
Warehouse = B
```

在同一 Tenant 内成立。

---

# 63. Draft Update 更需要同步校验

`update_purchase_order_draft`：

同样需要重新验证：

```text
Supplier
Warehouse
Catalog Items
```

不能假定创建时合法后永远合法。

---

# 64. Catalog Item 也要 Tenant 校验

`purchase_order_items.catalog_item_id`

当前设计没有形成足够强的同 Tenant 约束。

Service Role 下必须明确：

```text
Catalog Item tenant_id = PO tenant_id
```

---

# 65. P0-17B：部分收货会直接关闭整张采购单

Receive RPC 允许：

```text
received_qty = 0..ordered_qty
```

这是“部分收货”。

但之后无论是否全部收齐，都直接：

```text
PO.status = received
```

再 Post：

```text
只过账 received_qty > 0
↓
PO.status = posted
```

未收数量因此可能被默默丢弃。

---

# 66. 两种正确方案

### 简化版 Pilot

不支持部分收货：

```text
所有 Item 必须 received_qty = ordered_qty
```

才能进入：

```text
received
```

---

### 正式版

增加：

```text
partially_received
```

然后：

```text
部分收货
↓
继续收货
↓
全部完成
↓
received
```

或：

```text
Close Shortage
```

显式关闭未交货数量。

不能当前这样：

```text
部分收
→ 整单结束
```

---

# 67. 采购审批建议禁止自审

如果采购是敏感业务：

```text
submitted_by
```

不应等于：

```text
approved_by
```

建议沿用财务/病历：

```text
申请人 ≠ 审核人
```

在 RPC 强制。

---

# 68. Agent-06：寄养 Domain 分离设计是正确的

值得保留：

- 没有拿 `admissions` 冒充寄养；
- 新建独立 boarding_stays；
- 共享 rooms/cages；
- 住院/寄养使用同一个 Cage；
- 房位事务使用 `FOR UPDATE`；
- Daily Record 独立；
- Service Charge 独立；
- Migration 91 已实现 Checkout → Billing 集成。

这个方向符合之前规划。

---

# 69. P0-18：Boarding Idempotency 缺 Tenant Scope

至少两个函数：

```text
boarding_change_cage
boarding_checkout
```

查询 Idempotency 时有路径是：

```sql
WHERE idempotency_key = p_idempotency_key
```

没有先限制：

```text
tenant_id
```

如果不同 Tenant 产生相同 key：

存在：

```text
返回别的 Tenant 历史结果
```

的边界风险。

---

# 70. 修复

正确顺序：

```text
先锁定 / 加载 Stay
↓
得到 tenant_id
↓
idempotency lookup
WHERE tenant_id = stay.tenant_id
  AND key = ...
```

或 RPC 显式接受 Tenant 并验证。

---

# 71. P0-19：Boarding 入住缺客户/宠物关系完整验证

当前更关注：

```text
Cage
```

但还需要确保：

```text
customer 属于 tenant
pet 属于 tenant
pet.customer_id = customer
```

否则 Service Role Insert 可以生成逻辑不一致记录。

---

# 72. Boarding “预约入住”没有真正避免预约冲突

当前 Cage Lock 主要发生：

```text
check-in
```

而：

```text
planned
```

阶段没有完整日期区间冲突控制。

因此：

```text
同一笼位
8/10–8/12 预约 A
8/11–8/13 预约 B
```

可能都创建成功，

直到入住时才发现冲突。

---

# 73. 两种产品方案

### 方案 A：预订即占时间段

增加：

```text
expected check-in/out overlap
```

冲突校验。

---

### 方案 B：预约不绑定笼位

```text
planned
```

只记录需求，

到：

```text
check-in
```

再选 Cage。

对当前 Pilot，我更推荐：

```text
方案 B
```

实现更稳。

---

# 74. Boarding Charge 需要验证 Catalog

新增寄养服务时：

```text
catalog_item_id
unit_price
```

必须明确：

```text
项目属于当前 Tenant
```

最好：

```text
catalog_type = boarding
```

价格默认：

```text
从 Catalog Snapshot
```

如果允许手工改价：

必须独立权限 + reason。

---

# 75. Checkout 当前语义要区分“离店”和“已付款”

Migration 91：

```text
生成 Invoice
↓
Boarding checked_out
↓
释放 Cage
```

如果 Invoice 只是：

```text
draft / unpaid
```

那么产品正确文案应该：

```text
已办理离店，待支付账单已生成
```

而不是：

```text
结算完成
```

如果产品要求：

```text
必须付款后才能离店
```

则 Checkout 状态机需要进一步接 Payment。

两种模式必须明确，不能模糊。

---

# 76. P0-20：新 Domain 的 Direct Write 风险会被 Agent-01 Helper Regression 放大

Imaging / Followup / Boarding 的 RLS Policy 都调用：

```text
is_tenant_member
can_access_store
has_permission
```

而这三个 Helper 又被 Agent-01 后续 Migration 重写。

所以：

```text
Direct Write Policy 风险
+
Helper 权限语义回退
```

并不是两个独立小问题，而是会相互叠加。

因此修复顺序必须是：

```text
1 RPC ACL
2 Permission Helper
3 Direct Write Boundary
```

再审新模块。

---

# 77. Global Search：方向已正确，但仍有 Context 细节

上一轮要求：

```text
浏览器多表搜索
→ Hono /api/search
```

已经实现，这是明确进步。

并且：

- 服务端 Search；
- Input Escape；
- 前端 Abort/序列控制；
- Quick Action 权限意识；

都比上一版更好。

---

# 78. 但“授权 Scope”和“当前工作 Store”仍需要区分

例如 Tenant Owner：

```text
有整个 Tenant 权限
```

但当前顶部选择：

```text
Store A
```

Global Search 默认应该优先：

```text
Store A 业务对象
```

而不是因为：

```text
allowedStoreIds = 全 Tenant
```

就把 Store B/C 的预约、收费、就诊也混进来。

---

# 79. 正确搜索层次

```text
Authorization Scope
= 你最多能看什么

Requested Work Context
= 你现在正在哪个 Store 工作
```

默认搜索：

```text
先按 Current Store
```

用户明确切：

```text
全院搜索
```

才扩展到 Tenant。

---

# 80. Customer / Import 当前状态

## 已改善

Customer Detail：

```text
“就诊历史开发中”
```

已经被真实数据替换。

Customer 360 已有实际落地。

---

## 仍残留

Import Center：

```text
详情查看即将上线
```

仍属于生产占位。

而后端本身已经存在：

```text
GET /operations/imports/:id
```

因此这是低成本 P1 修复。

---

# 81. Mock Message 仍不建议放 Pilot 正式导航

当前消息模块仍可能显示：

```text
Mock Provider
```

虽然不会真实发送，这是安全的。

但产品上：

```text
正式导航里展示 Mock Provider
```

仍然不适合作为 Pilot 体验。

建议：

```text
Feature Flag 隐藏
```

直到真实 Provider 至少接一个。

---

# 82. DB Type Safety 仍不足

项目 Supabase Client 仍主要：

```text
untyped createClient
```

这轮一次新增：

- membership_discount_rules
- imaging_orders
- imaging_reports
- followup_tasks
- suppliers
- purchase_orders/items
- boarding_stays
- boarding_daily_records
- boarding_service_charges

等多张表。

TypeScript `typecheck PASS`：

```text
并不能证明数据库字段名、类型和关系一定正确。
```

---

# 83. 下一阶段建议

生成：

```text
Database Type
```

并使用：

```ts
createClient<Database>()
```

至少覆盖：

```text
关键新域 API
```

这不是本轮 Release Blocker，但应进入 P1 工程质量。

---

# 84. 时间/时区仍需要统一

本轮新模块尤其敏感：

```text
Followup scheduled_at
Imaging scheduled_at
Purchase expected_at
Boarding check-in/out
Tenant trial_ends_at
```

不能混用：

```text
Server Local
Browser Local
+08:00 hard-code
Tenant Timezone
```

统一：

```text
Store timezone
↓
Tenant timezone
↓
UTC storage
```

---

# 85. Agent-01 最终评分

## 已完成

```text
✅ 平台租户页面
✅ Tenant Detail
✅ Store Detail
✅ Suspend/Resume Command
✅ /api/user/context 基础
✅ Context UI
```

## 未真正完成

```text
❌ 权限 Helper 没保持 Hardened 语义
❌ Trial Context
❌ 当前 Store 权限有效集
❌ API Context Header
❌ 全页面 Store Refresh
❌ Password Recovery 收口
```

结论：

```text
UI / Feature：保留
IAM Core：回炉
```

---

# 86. Agent-02 最终评分

## 已完成

```text
✅ System Settings unique 修复
✅ Settings Registry
✅ Discount threshold 接 Billing
✅ Cashier Tendered/Applied
✅ Payment Context
✅ Membership Center
✅ Points
✅ Discount Rule
✅ Pricing Snapshot 方向
```

## 需补

```text
❌ 最终 RPC ACL
❌ Preview 不得决定真实会员折扣
❌ Remaining Settings 真生效
❌ Cross-Tenant Entity Validation
❌ Payment Method zero-active 行为
```

结论：

```text
大部分保留，集中补强。
```

---

# 87. Agent-03 最终评分

## 已完成

```text
✅ Imaging 全栈
✅ 报告版本基础
✅ Workbench 改造
✅ Lab Workflow DTO 基础
```

## 需补

```text
❌ Imaging Direct Write
❌ Entity Relationship
❌ Transition Race
❌ Published Revision Workflow
❌ Lab Review/Publish 语义
```

结论：

```text
模块保留，医疗状态机必须回炉一轮。
```

---

# 88. Agent-04 最终评分

## 已完成

```text
✅ Customer 360
✅ Visit History
✅ Followup Domain
✅ Followup UI/API/DB
```

## 需补

```text
❌ Direct Write Boundary
❌ Multi-Tenant Context
❌ Entity Relationship
❌ Timezone
❌ Cross-domain trigger
```

结论：

```text
结构正确，补安全和上下文即可。
```

---

# 89. Agent-05 最终评分

## 已完成

```text
✅ Supplier
✅ PO Header/Items
✅ State Machine
✅ RPC Command
✅ Idempotency
✅ Inventory Reuse
✅ Audit 方向
```

## 需补

```text
❌ Warehouse Store Validation
❌ Catalog Tenant Validation
❌ Partial Receiving
❌ Purchase Self Approval
```

结论：

```text
六组业务 Agent 中完成质量最好。
```

---

# 90. Agent-06 最终评分

## 已完成

```text
✅ Boarding 独立 Domain
✅ Cage 共用
✅ Double Occupancy Lock
✅ Daily Record
✅ Charges
✅ Checkout → Billing
```

## 需补

```text
❌ Direct Write Boundary
❌ Idempotency Tenant Scope
❌ Customer/Pet Validation
❌ Planned Booking Conflict
❌ Catalog Charge Validation
❌ Checkout/Payment 产品语义
```

结论：

```text
领域设计正确，事务边界还需补强。
```

---

# 91. Agent-07 最终评分

Agent-07 做好的：

```text
✅ 130 文件集成完整
✅ 新模块跨域基本接上
✅ Migration 90/91 有实际修复
✅ 交付文档齐全
✅ 文档记录 Typecheck/Build PASS
```

但作为最终质量门：

```text
❌ RPC ACL 结论过于乐观
❌ check:rpc-manifest 本身存在假绿
❌ 没发现 Permission Helper Regression
❌ 没发现 Trial Context
❌ 没发现 Global Permission Union
❌ 没发现 Request Context Header 缺失
❌ 没发现 Direct Write 新域回归
```

因此：

> **Agent-07 需要重新执行一次真正的 Security / Integration Closure，而不是继续新增功能。**

---

# 92. 本轮建议 P0 修复顺序

不要六个 Agent 再同时乱修所有问题。

建议拆成 4 条并发修复线。

---

## Fix Agent-A：Security / IAM

负责：

```text
Migration 92 RPC Final ACL
Migration 93 Permission Helper Restore
Trial Tenant
Context Effective Permissions
Axios Tenant/Store Headers
memberships[0] 清理
Password Recovery
```

这是最高优先。

---

## Fix Agent-B：Finance / Membership / Approval

负责：

```text
会员折扣 server authoritative
前端 threshold 动态化
Payment Context zero-active
Cross-Tenant Membership Validation
Remaining Settings
```

---

## Fix Agent-C：Medical / CRM

负责：

```text
Migration 94 Direct Write Lockdown
Imaging Context Validation
Imaging State Race
Lab Review/Publish
Followup Context
Followup Entity Validation
Timezone
```

---

## Fix Agent-D：Inventory / Boarding

负责：

```text
Purchase Warehouse/Store
Catalog Tenant
Partial Receive
Purchase Self Approval

Boarding Idempotency
Boarding Entity
Booking Conflict
Boarding Charge
Checkout Semantics
```

---

# 93. 最后再启动 Integrator-2

新的最终 Integrator 不开发功能。

只做：

```text
Final Migration Order
RPC ACL Runtime Check
Permission/RLS Model
Cross-Tenant Static Review
Build
Typecheck
Static Security Tests
文档真实状态
```

E2E 仍由现有 E2E Agent 独立继续。

---

# 94. 建议新 Migration 编号

当前最新：

```text
90
91
```

建议：

```text
92  RPC ACL Final Lockdown
93  Permission Helper Restore
94  New Domain Direct-Write Lockdown
95  Trial / Tenant Status Harmonization（如需 DB）
96  Purchasing Integrity
97  Boarding Integrity
98  Imaging Integrity
99  Final ACL/RLS Assertions（可选）
```

不要再回头编辑：

```text
54–91
```

保持 Migration 可审计性。

---

# 95. 新增真正的数据库安全 Gate

除了：

```text
check:rpc-manifest
```

新增：

```text
check:db-security
```

在应用 Migration 的测试数据库上执行。

至少验证：

## RPC

```text
Manifest 中 RPC：
authenticated EXECUTE = false
anon EXECUTE = false
service_role EXECUTE = true
```

---

## Sensitive Tables

例如：

```text
imaging_orders
imaging_reports
followup_tasks
boarding_stays
boarding_daily_records
boarding_service_charges
purchase_orders
purchase_order_items
```

验证：

```text
authenticated direct INSERT/UPDATE/DELETE
```

是否符合架构。

---

## Permission Helper

建立 SQL fixture：

```text
Tenant Owner
Store Manager
Doctor A
Cashier B
Platform Admin
Trial Tenant
Suspended Tenant
```

逐个断言：

```text
has_permission
can_access_store
is_tenant_member
```

---

# 96. Agent-07 的当前 Final Report 需要降级

当前文档不建议保留：

```text
全部安全检查通过
所有新 RPC service-role-only
```

这种措辞。

建议修订：

```text
Integration Build Passed
Static Source Review Pending Security Closure
```

---

# 97. `IMPLEMENTATION_STATUS.md` 建议状态

六个模块暂时统一标：

```text
Platform Tenant:
code_complete / security_fix_pending

Membership:
code_complete / integration_fix_pending

Imaging:
code_complete / workflow_security_fix_pending

Followup:
code_complete / context_security_fix_pending

Purchasing:
code_complete / integrity_fix_pending

Boarding:
code_complete / transaction_security_fix_pending
```

不要：

```text
verified
pilot_ready
```

---

# 98. E2E 本轮处理

再次明确：

```text
E2E = EXCLUDED
```

本次没有：

- 因 E2E 未完成扣分；
- 以 E2E 失败作为源码问题；
- 修改 E2E 断言；
- 要求重新执行 E2E 后才能完成本报告。

等当前 P0 源码修复进入新集成包后：

```text
再把新版本交给 E2E Agent
```

避免 E2E 在已知安全/状态问题版本上浪费大量验证时间。

---

# 99. Build / Typecheck 说明

Agent-07 交付文档记录：

```text
API tsc PASS
Vue typecheck PASS
check:rpc-manifest PASS
Vite build PASS
```

本次审计环境没有项目安装后的：

```text
pnpm / node_modules
```

所以这些结果：

```text
本报告视为“Agent 交付证据”
```

但：

```text
没有在本审计环境独立复跑。
```

特别是：

```text
check:rpc-manifest PASS
```

本报告已经证明：

> **这个 PASS 不能代表最终 ACL 安全。**

---

# 100. 最终结论

本轮并发开发值得保留。

它证明：

> 毛线球当前代码结构已经可以支持多个业务 Domain 并行扩展。

而且六个模块全部真正落地，开发速度提升是实质性的。

但是也暴露了一个新的工程阶段问题：

```text
单 Agent 功能正确
≠
最终 Migration / 权限 / 状态机整体正确
```

项目现在最需要的不是再开六个新需求。

而是先完成：

```text
Final Security Boundary
+
Permission/Context Consistency
+
Cross-Domain Transaction Integrity
```

这批 P0 修完以后，六个新模块大部分都可以直接保留，不需要重做。

当前建议状态：

```text
功能开发：通过
集成安全：未通过
产品数据完整性：部分通过
E2E：本轮未评审
Pilot Ready：暂不通过
```

下一步：

```text
4 个 Fix Agent 并发修复
↓
Integrator-2
↓
新完整代码包源码复审
↓
再进入 E2E / Runtime Pilot Gate
```

---

**审计报告结束**
