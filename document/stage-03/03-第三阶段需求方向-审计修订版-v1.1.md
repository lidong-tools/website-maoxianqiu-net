# 【毛线球】第三阶段（Stage 03）需求方向与执行基线

> 版本：v1.1（源码审计修订版）  
> 日期：2026-08-07  
> 替代：`03-第三阶段需求方向-三案合并-v1.0.md` 的执行基线地位  
> 权威产品细节仍以 PRD v0.5/v0.4 未冲突部分为准。

---

# 1. Stage 03 新定义

Stage 02 已完成大规模业务代码铺开，但源码审计确认基础层尚未完全收口。

Stage 03 不再从“S3.1 直接扩展”开始，而采用四段式：

```text
S3.0 Stage 02 Audit Closure
↓
S3.1 Pilot Ready
↓
S3.2 V1 Commercial
↓
S3.3 Scale & Differentiation
```

总体原则：

1. 安全作用域先于业务扩展；
2. 核心闭环先于功能数量；
3. 国家法规硬要求与行业最佳实践分开；
4. code_complete 不等于 verified；
5. C 端、会员、营销不阻塞 S3.1；
6. 所有 Stage 03 migration 建立在 S3.0 稳定 schema 之上。

---

# 2. S3.0 — Stage 02 Audit Closure

## 2.1 目标

把现有代码从“主体已铺开”提升为“基础可信”。

## 2.2 P0

| ID | 任务 | 验收 |
|---|---|---|
| S30-001 | 基线证明 | Git SHA / archive / health / deployment 对齐 |
| S30-002 | scoped permission 修复 | store role 不可用于 tenant-wide command |
| S30-003 | report-data store scope | 所有报表查询限定 allowedStoreIds |
| S30-004 | 宠物新增 UI | 客户详情可新增宠物，E2E 使用 UI |
| S30-005 | Picker 清场 | 正式表单无手填 UUID/技术 ID |
| S30-006 | 打印选择器 | 5 类打印均通过业务对象选择 |
| S30-007 | inventory reserve expiry | 过期不能 release 后再次 confirm |
| S30-008 | E2E hardening | A/B/C 缺 fixture 必须失败 |
| S30-009 | 就诊闭环顺序 | 支付与发药顺序形成产品规则 |
| S30-010 | typecheck | lint/typecheck/build 全绿 |
| S30-011 | 文档同步 | current 状态与代码一致 |

## 2.3 退出

S3.0 完成仅允许标记：

```text
code_complete
```

staging 未跑之前不得标记 `verified`。

## 2.4 S3.0 定向复审（S30-R01 ~ S30-R07）

S3.0 初审通过后，定向复审发现基础权限与安全边界仍需收口，批准以下 7 项（替代 S3.1 功能开发，S3.1 在 S3.0 通过前不启动）：

| R | 任务 | 验收 |
|---|---|---|
| S30-R01 | 数据库 scoped permission | migration 26 重建 `has_permission()`：tenant 上下文仅 tenant/system role；store 上下文仅目标 store role 或 tenant-wide role；store→tenant 提升负向测试 |
| S30-R02 | Hono role scope | `requireScopedPermission` 在计算 permission union 前校验 `role.scope`；禁止 scope=store + store_id=NULL 被当 tenant role；数据库非法 role assignment 约束/触发器 |
| S30-R03 | SECURITY DEFINER RPC 安全 | 高危 Command RPC（payment/refund/inventory/clinical/inpatient/diagnostics 等）revoke public/anon/authenticated + grant service_role；authenticated 直调 7 个 RPC 必须失败；不得依赖 SECURITY DEFINER+RLS 作为权限边界 |
| S30-R04 | employee.id / auth.users.id 语义修复 | 病历签署取消 EmployeePicker，强制当前登录 user.id；明确 employees.id 与 auth.users.id 字段语义，禁止混用 |
| S30-R05 | E2E A | UI 新建宠物（禁 api.post('/pets') 绕过）；签署步骤真实通过 |
| S30-R06 | Picker 最后清场 | inventory receipt 商品预留、inpatient nursing、print fallback、普通员工界面手填 UUID 清理 |
| S30-R07 | 证据和文档 | IMPLEMENTATION_STATUS / KNOWN_GAPS / RELEASE_CHECKLIST / 本文件更新；未验证项不标完成 |

退出条件：以上 7 项代码与测试文件落地，静态验证（typecheck/lint/build）全绿，提交 commit SHA + permalink + 完整源码 archive + /api/health 返回 + migration 26 + scoped permission 测试 + direct RPC 安全测试 + 更新后 closed-loop A。

在 S3.0 通过前，不批准任何 S3.1 任务。

---

# 3. S3.1 — Pilot Ready

## 3.1 合规 P0

### 病历

- 门急诊结束后 24h 归档；
- 住院出院后 3 日归档；
- 归档后直接编辑禁止；
- 特殊修改负责人批准 + amendment；
- 保存不少于 3 年；
- 电子病历身份认证、操作追溯；
- 可靠电子签名 Provider 预留。

### 处方

- 执业兽医备案；
- 签名留样/签章/电子签名备案；
- 当日有效；
- 特殊延长最长 3 天；
- 一式三联；
- 计算机开方满足纸质/可靠电子签名规则；
- 麻醉药单独处方且每张不超过一日量；
- 精神/毒性药单独处方；
- 普通处方保存 >=3 年；
- 麻醉/精神/毒性处方保存 >=5 年。

### 机构

- 许可证号、发证机关、诊疗范围；
- 新版电子证照字段/二维码预留；
- 门店与许可证关联；
- 执业人员备案；
- 公示能力。

### 年报

依据《动物诊疗机构管理办法》第三十条：

- 每年三月底前；
- 上年度诊疗活动；
- 支持生成、导出、提交状态和审计。

### 疫情与废弃物

- 疑似/染疫报告任务；
- 隔离/消毒留痕；
- treatment_restricted；
- 废弃物交接台账。

## 3.2 不作为全国法规硬 P0 的事项

以下转为 P1/可配置质控：

- 普通检验“录入人必须与审核人不同”；
- 所有处方强制第二人复核；
- 所有受控药品双人双锁。

高风险场景可由：

```text
tenant policy
local regulation
hospital SOP
```

升级为强制。

## 3.3 经营 P0

- tenant 初始化；
- 门店初始化；
- 日结；
- 对账；
- 审计查询后台；
- 最小回访任务；
- 低库存/近效期。

## 3.4 医疗 P0

- 护士任务自动生成；
- 超时/异常；
- 检验标本；
- 危急值确认；
- 报告引用病历；
- 住院每日病程；
- 出院结算；
- 基础打印。

## 3.5 S3.1 staging gate

必须执行：

```text
migration 0 -> latest
existing DB upgrade rehearsal
seed
RLS
multi-role permission
report data scope
inventory concurrency
payment/refund idempotency
closed-loop A
closed-loop B
closed-loop C
compliance E2E
```

通过后：

```text
verified
pilot_ready
```

---

# 4. S3.2 — V1 Commercial

P1：

- customer 360；
- duplicate merge；
- explainable segmentation；
- churn warning；
- follow-up operation；
- membership；
- stored value；
- points；
- package/card；
- coupon；
- referral；
- birthday campaign；
- C-end mini program；
- customer identity/authorization；
- published report viewing；
- reminder subscription；
- at least one real message provider；
- purchasing/supplier；
- insurance document pack；
- advanced reports；
- boarding；
- medication safety rule engine；
- configurable lab double review。

退出标准不以“页面完成”为依据，至少包含真实试点用户验证。

---

# 5. S3.3 — Scale & Differentiation

P2：

- AI SOAP draft；
- AI medical summary；
- reliable e-signature provider；
- regulator export/integration；
- SMS/WeChat/email multi-channel；
- LIS/PACS adapters；
- teleconsult data fields；
- report warehouse/precomputation；
- PDF archive；
- local print agent；
- supply-chain adapter fields。

AI 永远：

```text
assistant
not authority
human confirmation required
```

禁止：

- AI 自动诊断；
- AI 自动签署；
- AI 自动开方。

---

# 6. 合规修订记录

相较 v1.0：

1. 年度活动报告法规编号：
   `管理办法 §27` → `管理办法 §30`。
2. 检验同人审核禁止：
   从“法规 P0”改为“可配置临床质控 P1”。
3. 受控药双人双锁：
   从“734 直接要求”改为“风险控制 / 地方规范待确认”。
4. 疫情状态机：
   删除 `customed/customed_blocked`。
5. 许可证：
   增加 2025 新版电子证照字段预留。
6. 新增 S3.0：
   Stage 03 不再假设 Stage 02 已完全退出。

---

# 7. 执行顺序

```text
S3.0 security
  ├─ permission
  ├─ reports
  ├─ inventory
  └─ E2E/UI

S3.0 green
  ↓

S3.1 compliance migrations
  + tenant init
  + close/reconciliation
  + audit/follow-up

S3.1 code complete
  ↓

staging gates
  ↓

pilot
  ↓

S3.2 commercial features
```

S3.2 的产品设计可以提前，但其 migration / API / UI 大规模实施不能打断 S3.0/S3.1 的主线。

---

# 8. 状态规则

仅使用：

```text
not_started
in_development
code_complete
integration_pending
verified
production_ready
```

定义：

- `code_complete`：静态代码、单测、类型、build 满足；
- `integration_pending`：依赖 staging；
- `verified`：migration/RLS/E2E/业务场景均通过；
- `production_ready`：发布检查、配置、安全和回滚准备通过。

任何任务不得因为：

```text
有页面
有 API
有 SQL
有测试文件
```

直接标记 `verified`。

---

# 9. 下一批批准任务

当前只批准：

```text
S30-001 ~ S30-011
S30-R01 ~ S30-R07（定向复审，见 2.4）
```

S3.0 静态复审通过后，批准：

```text
S3.1 合规 P0
S3.1 经营 P0
S3.1 医疗补强
staging gates
```

在 S3.0 通过前，不启动 S3.1。

暂不批准：

```text
C-end implementation
membership full build
marketing engine
AI
device integration
advanced supply chain
```

这些保留在设计队列，不从主线抢开发资源。
