# 毛线球 SaaS — S3.1 Final Source Audit（全量包 12）

> 审计对象：`website-maoxianqiu-net-main (12).zip`  
> 审计性质：**全量源码最终审计**，本报告不再叠加任何旧增量包。  
> 范围：S3.1 IAM / Permission / Tenant-Store Context / Billing / Clinical / Inventory / Purchasing / Boarding / Migration / RLS / RPC。  
> E2E 与真实数据库 Runtime 不作为本报告源码结论的替代证据。

---

## 1. 最终结论

本次全量仓库确认：

```text
S3.1 Source Code Gate = PASS
```

此前连续多轮发现并修复的核心问题，在最新全量 Tree 中均未发现合并回归。

尤其：

```text
Migration Tree 唯一            ✅
旧 115 Source-Gate Migration   ✅ 已彻底删除
历史 Migration 不可变          ✅
RPC ACL Final Lockdown         ✅
Permission Helper Hardened     ✅
Trial Tenant Scope             ✅
Tenant / Store Context         ✅
Current Store Permission       ✅
Signed Progress Note Immutable ✅
Lab Workflow                   ✅
Cashier Core                   ✅
Purchasing Integrity           ✅
Boarding Integrity             ✅
Medical Loop SQL               ✅
Discharge Replay / Cage Safety ✅
```

因此 S3.1 不建议继续做功能级 Fix。

---

## 2. Migration Tree 正式通过

全量包中：

```text
Migration 文件：97
重复 Version：0
```

关键尾部：

```text
115_messaging_idempotency_lockdown
116_analytics_revenue_summary_rpc
117_s3_1_source_gate_fixes
118_messaging_cas_claim
119_import_awaiting_domain_apply
120_discharge_patient_replay_after_lock
```

此前一直无法由“增量 ZIP”证明删除的：

```text
115_s3_1_source_gate_fixes
```

在本次全量仓库中已经不存在。

### 结论

```text
✅ Migration Version Gate PASS
```

---

## 3. 历史 Migration 不可变正式通过

将当前全量仓库与较早稳定基线中的共同 Migration 逐文件比较：

```text
共同历史 Migration 被修改：0
```

此前曾被直接修改的：

```text
20260806000021_inpatient.sql
```

现在已经恢复历史版本。

后续修复由：

```text
117
120
```

Forward Migration 完成。

### 结论

```text
✅ Blank DB / Existing DB Upgrade 的源码演进方式正确
```

真实两条升级路径仍需 Runtime Gate 实际执行确认。

---

## 4. RPC ACL 源码边界通过

当前 Service-Role RPC Manifest：

```text
117 个唯一 RPC 名称
```

静态扫描 API Route：

```text
API route RPC names missing from manifest：0
```

Migration 92 对原有 Service-Role RPC 做最终 ACL Lockdown。

后新增：

```text
get_analytics_revenue_summary
```

由 Migration 116 单独：

```text
REVOKE public / anon / authenticated
GRANT service_role
```

### 结论

```text
✅ Source ACL Set 基本完整
```

### 但仍必须 Runtime 验证

仓库中的：

```text
api/scripts/check-rpc-manifest.ts
```

仍不是可靠 ACL 证明。

它的 SQL 覆盖检查会扫描普通字符串 literal，因此存在：

```text
false green
```

可能。

DB Runtime Gate 必须使用：

```text
pg_proc
has_function_privilege()
```

验证真实最终权限。

---

## 5. Permission / Tenant / Store Context 通过

当前体系已经形成：

```text
Toolbar Tenant / Store
↓
App Tenant Store
↓
Axios X-Tenant-Id / X-Store-Id
↓
Request Context
↓
Scoped Permission
↓
RLS / Command
```

前端权限：

```text
effectivePermissions(currentTenant/currentStore)
```

已经成为当前工作上下文的重要事实来源。

过去大量直接依赖：

```text
memberships[0]
```

的路径已经降级为：

```text
legacy fallback
```

而不是正常业务首选。

### 结论

```text
✅ S3.1 Context 主体通过
```

---

## 6. Signed Progress Note 通过

Forward Migration 117 已同时建立：

```text
RLS
+
Trigger
```

对于：

```text
status = signed
```

阻止普通：

```text
UPDATE
DELETE
```

不再只依赖 UI 或 Hono 状态机。

### 结论

```text
✅ 医疗病程签署后不可变
```

---

## 7. Discharge / Cage / Idempotency 通过

Migration 120 当前包含：

```text
tenant-scoped idempotency fast lookup
admission FOR UPDATE
after-lock idempotency replay
cage current_admission_id guard
```

并且上一轮遗漏的：

```sql
v_cage_rows int := 0;
```

在本次全量包中已经正确声明。

Cage Release：

```sql
where id = v_admission.cage_id
  and current_admission_id = p_admission_id
```

避免历史脏数据情况下误释放其它住院占用的 Cage。

### 结论

```text
✅ S3.1 Source 层通过
```

---

## 8. Medical Loop Test 已存在

仓库：

```text
supabase/tests/medical_loop_s3_1.sql
```

已覆盖核心医疗数据库闭环。

源码资产存在并不代表 Runtime PASS。

下一阶段必须在真实数据库运行。

---

## 9. S3.1 剩余事项均属于 Runtime / P1

源码层没有再发现需要新 Fix Agent 的 S3.1 P0。

后续：

```text
Blank DB → latest
Existing DB → latest
RPC ACL runtime assertion
RLS role matrix
medical_loop_s3_1.sql
Inventory concurrency
Finance idempotency
E2E
```

这些是下一阶段验证工作，而不是继续改 S3.1 架构。

---

## 10. S3.1 Final Verdict

```text
Source Code Gate       PASS
Migration Tree         PASS
Historical Immutability PASS
Security Architecture PASS at source level
Runtime DB Gate        PENDING
E2E                    NOT EVALUATED HERE
Pilot Ready            NOT YET
```

> 注意：整个仓库仍存在跨阶段的 Secret Hygiene Blocker，见 Mainline Final Source Audit。该问题不改变 S3.1 业务域本身的 Source PASS，但会阻止整个项目进入 Runtime/Pilot Gate。

**S3.1 Final Source Audit 结束。**
