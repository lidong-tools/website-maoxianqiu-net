# 毛线球 SaaS — S3.1 再次源码审计报告 v5

> 审计对象：`bb1643a0-9c25-4e75-ad64-f109e124c148.zip`  
> 该附件为 **15 文件增量包**，不是全量仓库。  
> 审计方式：叠加到上一版审计重建仓库后复核。  
> E2E：不作为本报告 Source Gate 结论依据。

---

# 1. 结论

S3.1 本轮继续修复总体通过。

本次新增：

```text
20260810000120_discharge_patient_replay_after_lock.sql
```

正确处理了上一轮指出的：

```text
两个并发 discharge 使用同一 idempotency key
```

第二请求在获取 admission 行锁后重新查询幂等结果，从而避免：

```text
ADMISSION_NOT_ADMITTED
```

而不能 replay 第一次结果的问题。

同时：

```text
Tenant Reactive Context
cachedUserId
Signed Progress Note
Medical Loop SQL
Forward Migration 117
```

均保持正确。

当前真正需要确认的只剩：

> **真实 Git 仓库是否已经删除旧 `20260810000115_s3_1_source_gate_fixes.sql`。**

---

# 2. ✅ discharge_patient 严格幂等 replay 已补

Migration 120：

```text
快路径：
先按 tenant + idempotency key 查询

↓ miss

锁 admission FOR UPDATE

↓
再次按 admission.tenant_id + key 查询

↓
若另一并发请求已完成：
直接 replay
```

这解决了：

```text
A/B 同 key

A 完成 discharge
B 等锁
B 获得锁
```

后 B 以前会直接：

```text
ADMISSION_NOT_ADMITTED
```

的问题。

### 结论

```text
✅ 通过
```

---

# 3. 🟡 discharge_patient 可再增加 Cage 一致性保护

当前释放 Cage：

```sql
update cages
set status = 'available',
    current_admission_id = null
where id = v_admission.cage_id;
```

建议长期进一步收紧为：

```text
where id = cage_id
and current_admission_id = admission_id
```

并检查：

```text
affected rows
```

避免数据库历史不一致时：

```text
Admission 仍指向 Cage A
但 Cage A 已被其他住院占用
↓
旧 Admission 出院误释放当前 Cage
```

正常业务路径下不应发生，所以当前属于：

```text
P1 Defense-in-Depth
```

不是本轮阻断项。

---

# 4. 🔴 当前增量包仍无法证明旧 Migration 115 已被删除

本次 ZIP 自身没有：

```text
115_s3_1_source_gate_fixes.sql
```

但因为它是增量包：

```text
ZIP 无法表达“删除旧文件”
```

叠加上一版完整仓库后，当前审计目录仍得到：

```text
20260810000115_messaging_idempotency_lockdown.sql
20260810000115_s3_1_source_gate_fixes.sql
```

因此自动检查仍显示：

```text
duplicate migration version = 20260810000115
```

---

# 5. 交付文档与审计附件之间存在证据差异

新的 Final Integration Report 声明：

```text
真实 Git 已删除旧 115
NO_DUPLICATE_VERSIONS
Migration Gate PASS
```

这个声明可能是真实的。

但当前提供的是：

```text
增量 ZIP
```

而不是：

```text
全量 Git Tree
```

所以本次附件无法独立证明删除已经发生。

### 正确验收方式

下次只需提供一次：

```text
全量代码包
```

或：

```text
真实 supabase/migrations 文件列表
```

确认只存在：

```text
115 messaging
116 analytics
117 s3_1_source_gate
118 messaging CAS
119 import
120 discharge replay
```

即可关闭这个问题。

---

# 6. ✅ E2E Setup 上一轮 SQL 问题已修

此前：

```text
employee_role_assignments
```

使用：

```sql
ON CONFLICT (
  tenant_id,
  employee_id,
  role_id,
  store_id
)
```

但表上没有对应 Unique Constraint。

本轮改为：

```sql
INSERT ...
WHERE NOT EXISTS (
  ...
  store_id IS NULL
)
```

与 Tenant-wide Role 的 NULL 语义一致。

### 结论

```text
✅ 修复正确
```

---

# 7. S3.1 当前状态

| 项目 | 状态 |
|---|---|
| Signed Progress Note Immutable | ✅ |
| Tenant/Store Context | ✅ 主体通过 |
| cachedUserId | ✅ |
| Medical Loop SQL | ✅ |
| Forward Migration 117 | ✅ |
| discharge replay 120 | ✅ |
| E2E setup employee role seed | ✅ |
| Migration 115 删除 | ⚠️ 增量包无法证明 |
| Runtime DB | ⏳ |
| E2E Runtime | 本报告不评 |

---

# 8. 最终判定

如果真实 Git 仓库已经删除：

```text
20260810000115_s3_1_source_gate_fixes.sql
```

则：

```text
S3.1 Source Code Gate = PASS
```

S3.1 已经不需要继续开发新的 Fix 功能。

下一步就是：

```text
Blank DB → latest
Existing DB → latest
medical_loop_s3_1.sql
RPC ACL / RLS Runtime Assertions
E2E
```

如果真实 Git 仍保留旧 115：

```text
Migration Gate = FAIL
```

必须先删除/迁移正确。

---

**S3.1 再次审计结束。**
