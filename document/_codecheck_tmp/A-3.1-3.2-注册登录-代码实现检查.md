# 3.1 系统注册 / 3.2 系统登录 —— 代码实现检查报告

> 依据需求报告：document/_reqmap_tmp/A-3.1-3.2-注册登录.md
> 检查方式：只读代码检查（前端视图/路由/API、后端 Hono 路由、Supabase 迁移/RPC/RLS），未修改任何业务代码
> 需求点总数：7 ｜ 已实现 4 / 不完整实现 1 / 未实现 2

---

## 3.1-01 安装向导 → SaaS 租户初始化向导（含初始化进度与安装完成等待）

- 功能界面：✅已实现 — `apps/maoxianqiu/src/views/system/tenant-init/index.vue`（L178-272 页面模板：左"目标租户"选择+状态卡片、右"初始化参数"表单）；路由注册 `apps/maoxianqiu/src/router/modules/system.ts` L46-55（`/system/tenant-init`，title 新建医院，`meta.auth: 'tenant.initialize'`）
- 界面功能实现：✅ — 表单字段 tenantSlug/tenantName/storeName/storeCode/ownerUserId/ownerName/ownerPhone/timezone（index.vue L39-48）；初始化状态标签 not_started/pending/running/completed/failed（L65-71）；pending/running 自动 3s 轮询状态（L116-128）；失败显示 attempts/lastError 并提供"重新初始化"按钮（L205-211、L264-266）；完成显示首店名称/编码（L212-217）。为"单页参数表单+一键执行+状态轮询"，非竞品分步安装向导（与 SaaS 化设计一致）
- 业务流转流程：✅ —
  - `POST /api/tenants/initialize`：`api/routes/tenants.ts` L47-130（校验 → 平台管理员可先建 tenants 行 L57-79 → `requireScopedPermission(tenant.initialize)` L90 → 调 `initialize_tenant` RPC L92-104 → 写审计 `tenant.initialize` L117-127）
  - `GET /api/tenants/:id/initialization`：tenants.ts L221-247 + `get_tenant_initialization` RPC（`supabase/migrations/20260809000036_tenant_init_rpc.sql` L353-395，未初始化返回 not_started）
  - `initialize_tenant` RPC（migration 36 L25-345）：单事务依次创建 首店(L189)→tenant_owner 员工+角色分配 store_id=NULL(L205-215)→租户成员关系(L218-220)→默认仓库 WH-DEFAULT(L223-225)→5 种支付上下文 cash/card/wechat/alipay/other(L228-236)→Pilot 基础字典 species/breed/color/gender/pet_size(L239-267)→3 套打印设置 58mm/80mm/a4(L270-276)→状态 completed+审计 complete(L279-290)；失败置 failed+审计 fail+幂等缓存(L292-315)
  - 幂等/可恢复：idempotency_records 双检（L89-110）+ `pg_advisory_xact_lock`（L99-100）+ 表幂等键唯一与部分唯一索引 `idx_tenant_init_single_active`（migration 35 L48、L56-58）+ failed 自动重试最多 5 次（L131-149）+ TENANT_INIT_IN_PROGRESS/MAX_RETRIES 错误（tenants.ts L107-113）
  - 审计事件 start/retry/complete/fail 全部写入 audit_logs（migration 36 L181-183、L146-148、L288-290、L301-303）；表结构/RLS/权限码 seed 见 migration 35（tenant_initializations/payment_contexts/print_settings/base_dictionaries）与 migration 37（tenant.initialize、tenant.initialization.read 权限码 + tenant_owner scope=tenant 授权 + service-role-only）
- 判定：已实现
- 说明：前端向导页、初始化 API、RPC 状态机、幂等重试、审计、默认仓库/支付/字典/打印配置、tenant_owner 角色约束（scope=tenant 且 ERA.store_id=NULL，migration 37 L92-105 + RPC L213-215）全部落地。待校验点全部得到确定结论。仅"向导"形态从分步向导简化为单页表单+轮询，属 SaaS 化预期而非缺口。

## 3.1-02 16 位激活密钥与密钥激活界面

- 功能界面：❌未实现 — 无任何"激活密钥"界面（登录页 `views/login.vue`、初始化向导页均无）
- 界面功能实现：❌ — 无密钥输入/校验界面
- 业务流转流程：⚠️ 替代方案（SaaS 订阅与租户状态）部分落地 —
  - tenants 表字段：`supabase/migrations/20260806000008_tenants.sql` L18-19 含 `status(active/trial/suspended)` + `trial_ends_at`；**无 subscription（订阅方案）字段**（PRD §4.2 规划的"订阅方案"未落地）
  - 停用后新业务拦截（双向）：Hono `api/lib/permission.ts` resolveScopedAccess L425-440（suspended/trial 过期 → 403，消息"该租户已停用/试用已到期"）；RLS helper `is_tenant_business_active`（`supabase/migrations/20260810000093_permission_helper_restore.sql` L25-38，is_tenant_member/can_access_store/has_permission 均叠加）；`/api/me/context` 过滤（`api/lib/me-context.ts` L146-154、L327-344）
  - 平台租户管理（试用状态展示/停用/恢复）：`views/system/tenants/index.vue`（状态筛选 L184-202、停用/恢复 L120-159）+ `tenants/detail.vue`；权限码 platform.tenant.* + suspend_tenant/resume_tenant RPC（`supabase/migrations/20260810000054_platform_tenant_mgmt.sql`、`20260810000095_trial_tenant_status_harmonization.sql`）
  - 订阅计费闭环：全库未检索到任何"订阅方案/订阅计费/套餐续费"代码
  - 激活码逻辑：全仓检索"激活码/激活密钥/16位/activate"无任何业务命中（仅 vue 生命周期钩子 onActivated 无关）
- 判定：未实现（符合文档"有意淘汰"设计，见 07 矩阵 L19：16 位激活密钥→订阅与租户状态）
- 说明：三个待校验点结论：①status/trial_ends_at 存在、subscription 字段缺失；②停用拦截已 Hono+RLS 双向落地；③确认系统内无任何"16 位激活密钥"校验逻辑。缺口=付费订阅计费闭环未实现。

## 3.1-03 新增 200 种处方套餐（安装包宣传文案）

- 功能界面：❌未实现 — 无"处方套餐"展示界面（注册/初始化流程无套餐内容）
- 界面功能实现：❌ — 无
- 业务流转流程：❌ — 无处方套餐种子数据：
  - `supabase/seed.sql` 与全部 migrations 无任何 `service_packages` 预置/种子插入；`supabase/migrations/20260810000222_service_packages.sql` 仅建表（注释示例"洗护 10 次卡/疫苗年度套餐/复诊包"），无数据
  - "处方"仅作为权限码（seed.sql prescription.create/dispense/issue 等）与打印模板类型 `type in ('invoice','prescription',...)`（`supabase/migrations/20260806000018_operations.sql` L224），非"200 种处方套餐"
  - V1 套餐（会员/营销组合项目）存在：service_packages 表 + `views/marketing/packages/index.vue`，但与注册/初始化无关
- 判定：未实现
- 说明：与需求报告一致——"200 种处方套餐"属手册安装包广告性文案，代码中无任何依据；若验收要求预置处方套餐种子，需与项目负责人确认。

## 3.1-04 医院注册信息表单（医院名称/医院地址/详细地址/院长/院长手机/验证码）

- 功能界面：⚠️不完整 — 无 B 端公开注册页；医院信息分散承载于：租户初始化向导（`views/system/tenant-init/index.vue`）、系统设置-医院信息（`views/system/settings/index.vue` L568-588 医院信息 Tab）、门店管理（`views/system/store/index.vue` + `components/StoreForm.vue`）
- 界面功能实现：⚠️ —
  - 医院名称：✅ 初始化入参 tenantName + 系统设置"医院信息"表单 name（settings/index.vue L34-40、L570-571）
  - 院长/院长手机：⚠️ 由"所有者"承接——初始化入参 ownerName/ownerPhone（tenant-init/index.vue L250-255），无"院长"称谓字段
  - 医院地址/详细地址：❌ 租户表无地址列（migration 08 仅 slug/name/short_name/logo/timezone/currency/locale/status/trial_ends_at）；初始化向导入参无地址；系统设置-医院信息无地址；仅门店表单有单一"地址"字段（StoreForm.vue L115-117；`api/modules/store.ts` L44-98 create/update 均支持 address；门店列表含地址列 store/index.vue L33）——"医院地址"与"详细地址"两级结构均缺失
  - 验证码：❌ 无 B 端注册验证码（Pilot 关闭公开注册）：login.vue 仅 Login/ResetPassword 双表单；`components/AppAccountForm/register.vue` 组件存在但全仓无任何引用；登录组件提示"没有账号？请联系医院管理员邀请。"（AppAccountForm/login.vue L104）
- 业务流转流程：⚠️ — 租户创建（POST /api/tenants/initialize，平台管理员新建 L57-79）+ 门店创建（浏览器直连 supabase stores.insert，含 address/phone，`api/modules/store.ts` L52-59）链路打通；验证码流程无对应（verification_challenges 仅 C 端门户身份，B 端无）
- 判定：不完整实现
- 说明：医院名称/联系人/电话已由租户+门店模型承接（对应 PRD §4.2/4.3）；"医院地址/详细地址"仅门店单地址字段，医院级地址缺失；B 端验证码按设计不存在（与需求报告 3.1-04 覆盖状态"部分覆盖"一致，验证码缺失属符合 Pilot 关闭公开注册的预期差异）。

## 3.1-05 设置管理员账号（登陆名/姓名/密码/确认密码）

- 功能界面：✅已实现 — 用户/员工管理页 `views/system/user/index.vue`（列表含账号/姓名/手机号/门店/角色/状态 L132-178；状态含 invited"邀请中" L166）；路由 `/system/user`（`router/modules/system.ts` L6-14，meta.auth `system:user:manage`）
- 界面功能实现：✅ — 新增用户弹窗 UserForm（账号/初始密码≥6位/姓名/手机号/门店/角色，`components/UserForm.vue` L142-161）；编辑、改角色（RoleChangeForm）、停用/启用、管理员重置密码（PasswordForm）等操作入口齐全
- 业务流转流程：✅ —
  - 管理员建号 `POST /api/user/create`：`api/routes/user.ts` L33-109（createUser email_confirm=true + 初始密码 L65-74 → `invite_employee` RPC 事务化建 tenant_membership+employee+store_assignment+role_assignment L81-92 → 失败补偿删除 auth 用户 MXQ-3009 L93-97）
  - 员工邀请端点 `POST /api/employees/invite`：`api/routes/employees.ts` L41-（权限 employee.invite，同走 invite_employee RPC）；前端封装 `api/modules/user.ts` L135-161
  - 管理员重置密码 `POST /api/user/reset-password`：`api/routes/user.ts` L117-149（system.user.resetPassword 权限）
  - tenant_owner 创建：initialize_tenant RPC 4.2（migration 36 L205-215，tenant_owner 角色分配 store_id=NULL；migration 37 L92-105 授予 tenant_owner scope=tenant）
  - Auth 用户 ↔ employee 绑定：employees.user_id（RPC L205-211）
  - 登录页无公开"注册新帐号"入口：✅（login.vue 双表单切换 L22/L65-76；无 register 引用）
- 判定：已实现
- 说明：竞品"注册时设置管理员账号"升级为"租户初始化 owner + 管理员建号/邀请员工（含初始密码）+ 重置密码链路"模式，与文档（PRD-v0.5 §2.1-6、08 §15、sprint-2-1 L258-317）完全一致；登录页提示"没有账号？请联系医院管理员邀请"落地。

## 3.2-01 账号密码登录（含忘记密码、记住我）

- 功能界面：✅已实现 — `apps/maoxianqiu/src/views/login.vue`（Login/ResetPassword 双表单切换 L22、L65-76）；登录表单 `components/AppAccountForm/login.vue`（用户名/密码/记住我/忘记密码 L76-105）；密码重置回调页 `views/auth/reset-password.vue`（新密码/确认密码表单 L92-119、无效链接兜底 L121-134）；路由 `/login`、`/auth/reset-password`（`router/routes.ts` L25-51）
- 界面功能实现：✅ —
  - 记住我：login.vue 组件 L41-49（remember 勾选时把用户名写入 localStorage `login_account`，否则移除——记住用户名而非自动登录）
  - 忘记密码：登录组件 L96-98 触发 → `supabase.auth.resetPasswordForEmail(account, { redirectTo: '/auth/reset-password' })`（`store/modules/app/account.ts` L123-132，P0-08 显式 redirectTo）→ 邮件回跳 reset-password.vue：校验 type=recovery/token（L56-61）+ PASSWORD_RECOVERY 事件监听（L63-68）+ sessionStorage 兜底标志 `mxq:recovery-pending`（`lib/supabase.ts` L16-19、reset-password.vue L72-75）→ `updateUser({password})` 改密（L37）→ 登出回登录页
- 业务流转流程：✅ —
  - `supabase.auth.signInWithPassword`（account.ts L89-92）→ syncFromSession（token/account 入 localStorage L38-60）→ loadProfile → `useAppTenantStore().initContext()` → getPermissions（L96-101）
  - 401 未登录重定向：路由守卫 `router/guards.ts` L74-84（未登录且非 login/authResetPassword → 跳 login 并带 redirect）；HTTP 401 → `requestLogout()` 回登录（`api/index.ts` L67-69）
  - 登录页无公开注册入口：✅（register.vue 无引用；E2E 注释明确"maoxianqiu 登录组件已移除登录方式切换 tab"，`e2e/tests/login.spec.ts` L15）
- 判定：已实现
- 说明：账号密码登录、会话管理、401 重定向、记住我、忘记密码邮件链路全部打通并有 E2E 覆盖（e2e/tests/login.spec.ts 登录渲染+登录流程 L10-38）；"注册时设置的管理员账号"语义由邀请制替代（见 3.1-05）。

## 3.2-02 登录后模块导航（模块划分）

- 功能界面：✅已实现 — 动态菜单注册 `apps/maoxianqiu/src/router/routes.ts` L86-230：14 个顶级分组（客户宠物/营销增长/目录价目/库存管理/收费收银/诊疗核心/住院管理/疫苗检验/运营管理/经营分析/监管运营/日结对账/客户门户/系统管理）；`router/modules/` 共 16 个业务模块文件（analytics/billing/catalog/clinical/closing/crm/crm-growth/diagnostics/inpatient/insurance/inventory/marketing/medication-safety/operations/portal/regulatory + system）
- 界面功能实现：✅ — 每个模块路由均配置 `meta.auth` 权限码（如 system.ts：systemUser `system:user:manage`、systemTenantInit `tenant.initialize`、systemTenants `platform.tenant.list`；operations.ts 消息中心 `message.manage`）；登录后按权限过滤生成菜单（guards.ts L40-47 `generateRoutesAtFront(asyncRoutes)`；L89-100 setupRedirectAuthChildrenRoute 用 `useAppAuth().auth(meta.auth)` 过滤）
- 业务流转流程：✅ — 登录 → initContext + getPermissions（account.ts L98-100）→ 守卫动态注册路由；切租户/门店 watch 同步有效权限（account.ts L221-226）；role 维度菜单可见性由 meta.auth + 权限过滤机制保证
- 判定：已实现
- 说明：模块划分与竞品 10 模块存在差异（14 个顶级分组；短信管理并入"运营管理→消息中心" operations.ts L84-90、产品管理拆为"目录价目/收费收银"等），属文档已声明的 SaaS 重构正常差异（07 矩阵 L24-65），非缺陷。需求报告引用的"9 个模块对应 9 个 router/modules"描述已过时（实际 16 个业务模块文件）。各岗位角色登录后菜单可见性验证有 E2E 基础登录测试覆盖。

---

## 汇总

| 需求点 | 界面 | 界面功能 | 业务流转 | 判定 |
|---|---|---|---|---|
| 3.1-01 租户初始化向导 | ✅ | ✅ | ✅ | 已实现 |
| 3.1-02 16 位激活密钥 | ❌ | ❌ | ⚠️ | 未实现（有意淘汰，替代部分落地） |
| 3.1-03 200 种处方套餐 | ❌ | ❌ | ❌ | 未实现 |
| 3.1-04 医院注册信息表单 | ⚠️ | ⚠️ | ⚠️ | 不完整实现 |
| 3.1-05 设置管理员账号 | ✅ | ✅ | ✅ | 已实现（升级为邀请制） |
| 3.2-01 账号密码登录 | ✅ | ✅ | ✅ | 已实现 |
| 3.2-02 登录后模块导航 | ✅ | ✅ | ✅ | 已实现 |

### 主要差距说明

1. **3.1-02 订阅计费闭环缺失**（未实现但有替代）：激活密钥按文档设计淘汰，替代方案"租户状态 + 停用拦截 + 平台租户管理"已双向落地（Hono resolveScopedAccess + RLS is_tenant_business_active + me/context 过滤），但 tenants 表无 `subscription` 字段、全库无订阅/计费/续费代码，"付费订阅计费闭环"是唯一未落地缺口。
2. **3.1-03 无任何"200 种处方套餐"依据**：seed.sql/migrations 无套餐种子数据，"处方"仅存在于权限码与打印模板类型；V1 套餐（service_packages）为会员营销组合项目且无预置数据。若验收要求预置处方套餐需项目负责人确认。
3. **3.1-04 医院级地址与 B 端验证码缺失**：医院名称/院长(所有者)/电话已承接，但"医院地址/详细地址"仅门店表单单一地址字段（医院级地址、详细地址两级缺失）；B 端注册验证码不存在（符合 Pilot 关闭公开注册设计，登录页仅"账号登录+忘记密码"，提示联系管理员邀请）。
4. **3.2-02 文档描述过时**：需求报告引用的"9 个模块/9 个 router/modules"与实际（14 个顶级分组、16 个业务模块文件）不符，建议同步更新需求文档；模块划分差异属 SaaS 重构正常差异。
