# 3.4 系统管理 —— 代码实现检查报告

> 依据需求报告：document/_reqmap_tmp/C-3.4-系统管理.md
> 需求点总数：17 ｜ 已实现 8 / 不完整实现 6 / 未实现 3

> 说明：业务前端位于 `apps/maoxianqiu/src/views/`（非 apps/core），后端 Hono API 位于 `api/routes/`，数据库迁移位于 `supabase/migrations/`。本次检查为只读代码核对，未做运行时验证。

## 3.4.1.1-01 机构信息维护

- 功能界面：✅已实现 — 门店管理 `apps/maoxianqiu/src/views/system/store/index.vue`（列表/新增/编辑/归档/恢复，L55-148）；门店详情 `store/detail.vue`（概览/人员/仓库 Tab，L44-100）；系统设置-医院信息 Tab `system/settings/index.vue` L568-591（租户名称/简称/时区/币种/语言）；平台租户列表 `system/tenants/index.vue`（L46-70 医院名称/简称/状态/门店数/员工数）、租户详情 `tenants/detail.vue`（概览/门店/人员 Tab，L291-296）。
- 界面功能实现：✅ — StoreForm.vue L109-120（名称/编码/地址/电话/状态，必填名称）；settings 门店营业 Tab L594-633（时区 + 周一~周日营业时间维护）。
- 业务流转流程：✅ — stores 表 + `archive_store`/`restore_store` RPC（store/index.vue L120-148 调用）；tenants 表 `supabase/migrations/20260806000008_tenants.sql`（name/short_name/timezone/currency/logo_file_id 等）；租户初始化 `system/tenant-init/index.vue`（首店/tenant_owner/仓库/支付/字典/打印设置一键初始化）。
- 判定：已实现
- 说明：竞品"机构信息"以"租户+门店"两级维护升级落地；门店基础字段（名称/电话/地址/状态）+ 营业时间（settings-门店营业 Tab）齐全；租户/门店联系人、Logo 上传 UI 见 3.4.2.3-05。

## 3.4.1.2-01 用户角色管理

- 功能界面：✅已实现 — `apps/maoxianqiu/src/views/system/role/index.vue`（角色列表：名称/编码/描述/权限数，L23-43；新增/编辑/删除，L82-109）+ `role/components/RoleForm.vue`（角色编码/名称/描述 + 权限复选框组 L132-141）。
- 界面功能实现：✅ — 新增/编辑角色弹窗、非系统角色可删除（index.vue L136 下拉仅 `!is_system`）、系统内置角色只读（RoleForm L136 复选框 `:disabled="model.isSystem"`）；内置角色 seed（system_admin/tenant_owner/store_manager/staff/cashier/doctor/nurse 等，见 `supabase/migrations/20260806000003_rbac.sql`）。
- 业务流转流程：✅ — `api/routes/roles.ts` `POST /roles/replace-permissions`（L32-72，权限 `role.permission.update`，事务化 `replace_role_permissions` RPC 同步 role_permissions 与 roles.permissions 数组，系统角色后端拦截）；roles/permissions/user_roles/role_permissions 表（20260806000003_rbac.sql）。
- 判定：已实现
- 说明：自定义角色权限编辑（权限码勾选 + 系统角色只读）完整；角色码不可改（RoleForm L122 `:disabled="model.id !== ''"`）。

## 3.4.1.3-01 权限管理（为用户/角色分配权限）

- 功能界面：✅已实现 — 权限码清单 `apps/maoxianqiu/src/views/system/permissions.ts`（约 100+ 权限码，覆盖 user/role/store/employee/catalog/clinical/billing/inventory/supplier/purchase/operations/settings 等域，L3-120+）；角色-权限绑定界面见 3.4.1.2-01 RoleForm 权限复选框。
- 界面功能实现：✅ — RoleForm 按权限码清单渲染勾选（L133-137），编辑非系统角色走 `replacePermissions`（RoleForm L92-97）；前端菜单/按钮按 `auth('permission.code')` 过滤（settings/index.vue L18-19、PermissionButton 组件贯穿各页）。
- 业务流转流程：✅ — 后端 `api/lib/permission.ts`（`loadPermissions` L55 / `requireScopedPermission` L601 / `resolveScopedAccess` L282，scope 感知）；数据库 `has_permission(p_tenant_id,p_store_id,p_permission)` 定义于 `supabase/migrations/20260806000010_rls_helpers.sql` L86、`20260806000013_iam_completion.sql` L11、`20260807000026_scoped_permission_hardening.sql` L26、`20260810000054_platform_tenant_mgmt.sql` L187，最新强化 `20260810000093_permission_helper_restore.sql` L44（platform_admin 短路 + is_tenant_member + can_access_store）；平台角色隔离 platform_user_roles 在多份迁移/RLS 测试中被引用（如 `20260810000266_portal_appointment.sql`、`supabase/tests/rls_tenant_store.sql`）。
- 判定：已实现
- 说明：权限粒度"资源+动作+范围"（store/tenant/platform）落地，RLS 全局引用 has_permission。

## 3.4.1.4-01 系统用户管理（新建账号/停用/重置密码）

- 功能界面：✅已实现 — `apps/maoxianqiu/src/views/system/user/index.vue`（列表：账号/姓名/手机号/门店/角色/状态，L133-179；新增用户/编辑资料/修改角色/重置密码/停用启用/移出门店下拉，L487-503）+ `user/components/UserForm.vue`、`PasswordForm.vue`、`RoleChangeForm.vue`。
- 界面功能实现：✅ — 新增用户（账号+密码+姓名+手机号+门店+角色，UserForm L20-42）、修改角色（RoleChangeForm）、管理员重置密码（PasswordForm，index.vue L344-376）、启停账号（L382-410 新模型走 setStatus RPC / 旧模型 profiles.update）、门店筛选（L447-449）。
- 业务流转流程：✅ — `api/routes/employees.ts`：`POST /invite`（L41，权限 employee.invite，建 auth 用户 → `invite_employee` RPC 事务化建员工/分配/角色，L94-106）、`/set-status` L143、`/assign-store` L200、`/remove-store` L263、`/change-role` L309；权限码 employee.create/invite/disable/enable/assignStore/changeRole（permissions.ts L16-22）。
- 判定：已实现
- 说明：员工邀请（invite_employee RPC）链路完整；登录后按权限过滤菜单/按钮已由 auth() 指令实现。

## 3.4.2.1-01 供应商管理

- 功能界面：✅已实现 — `apps/maoxianqiu/src/views/inventory/suppliers/index.vue`（列表：编码/名称/联系人/电话/账期/状态/类别，L50-84；新增/编辑/停用启用按钮 L285-320；详情抽屉+采购历史 L337-369）。
- 界面功能实现：✅ — 新增/编辑弹窗含名称（必填）/联系人/电话/账期/统一信用代码/类别（逗号分隔）/地址/备注（L372-408）；启停切换（onToggleStatus L244-258）；供应商为租户级主数据（L108-113 切租户重载）。
- 业务流转流程：✅ — suppliers 表 `supabase/migrations/20260810000065_suppliers.sql`（L10-27：supplier_no/name/contact_name/phone/address/unified_credit_code/payment_terms/status/categories/notes，租户级唯一编码；L42-45 RLS 仅租户成员可读、写入走 Hono Command+审计；L50-55 权限码 supplier.view/supplier.manage）；与采购链路关联：详情抽屉直查 `purchase_orders`（suppliers/index.vue L143-152），采购申请转 PO 需指定供应商（stage-04 handoff AGENT-07 要求）。
- 判定：已实现
- 说明：需求报告标注"供应商独立维护界面证据不足（待确认）"——本次核实**已实现**：维护界面完整（含联系人/电话/账期/状态/供应品类/备注字段）+ 采购历史联动。

## 3.4.2.2-01 数据导入（Excel 模板下载 + 批量导入）

- 功能界面：✅已实现 — 导入中心 `apps/maoxianqiu/src/views/operations/imports/index.vue`（任务列表：全部/进行中/历史/失败 Tab，L28-34；详情抽屉 L129-139、错误明细抽屉 L141-147）+ 导入向导 `apps/maoxianqiu/src/components/imports/ImportWizard.vue`（5 步：选择类型→字段映射→预览校验→确认执行→查看结果，L32-38）。
- 界面功能实现：✅ — 模板下载（ImportWizard L86-93 `downloadTemplate`）、文件上传（L95-123）、字段映射+去重策略（L125-146，skip/update/create_duplicate）、预览（前 N 行）、校验（L148-161 错误分组/样例错误）、执行（L163-177）、错误明细（L179-192）；导入类型 5 类：customer/pet/catalog-item/employee/opening-stock（`apps/maoxianqiu/src/types/imports.ts` L6、L99-107，商品/药品统一并入 catalog-item 模板，处置/预防/美容项目同样经 catalog-item 导入）。
- 业务流转流程：✅ — import_jobs 状态机（uploaded/mapped/validated/queued/pending/processing/completed/failed/cancelled，`supabase/migrations/20260810000100_import_center_v2.sql` L29-37）；`api/routes/imports.ts`：POST /upload L254、/:id/mapping L336、/:id/validate L394、/:id/start L478（L499-527 幂等：completed/failed/awaiting_domain_apply 重复 start 直接返回 `idempotent:true`，validated→processing 条件更新防并发）、/:id/cancel；`api/services/imports/execute.ts` 按类型执行（客户按手机号/编号去重、宠物按主人+名/芯片、商品按编码；L55-100+）；employee 导入走 `employee_invite_imports` 待邀请队列（不绕过 IAM，execute.ts L8-9、20260810000100 L115-143），opening-stock 走 `opening_stock_import_requests` 命令队列（L80-108）。
- 判定：已实现
- 说明：需求报告引用的 S3.2 审计"执行幂等/闭环未通过"问题已由 stage-04 收口（imports.ts 幂等分支 + apply 消费 + employee/opening-stock 命令队列），本仓库代码为收口后版本；运行时验证属部署侧 Gate（runtime_pending），不影响代码实现判定。

## 3.4.2.3-01 系统参数设置

- 功能界面：✅已实现 — 系统设置中心 `apps/maoxianqiu/src/views/system/settings/index.vue`（6 Tab：医院信息/门店营业/业务规则/支付/打印/字典，L556-565）。
- 界面功能实现：✅ — 业务规则 Tab L636-690：展示每条生效配置的来源标签（本店覆盖/医院默认/系统默认，SOURCE_LABELS L198-202 + item.source 徽标 L656-664），支持"保存为本店覆盖"（L676）、"保存为医院默认"（L679）、"删除覆盖"恢复继承（L682-684）；支付方式维护（L693-722）、打印设置维护（L725-754）、基础字典维护（物种/品种/毛色，L757-789）。
- 业务流转流程：✅ — system_settings 表 + `GET /settings/effective`（`api/routes/settings.ts` L56-107：store override → tenant default → system default 合并并标注 source）；`PUT /settings/:namespace/:key`（L164-224，`SETTING_REGISTRY` 强类型校验 L29-35，拒绝未知键）、`DELETE /settings/:namespace/:key/override`（L233-264 删除覆盖恢复继承）；变更写审计（L224 等 audit action）；print_settings/payment_contexts/base_dictionaries 均由租户初始化幂等写入（`20260809000035_tenant_init_schema.sql`）。
- 判定：已实现
- 说明：需求报告标注"参数中心统一维护页面 stage-03 列为缺失（待确认）"——本次核实**已实现**：/system/settings 页面存在且展示配置来源、支持门店覆盖/租户默认/删除覆盖、Registry 约束 + 审计齐全。

## 3.4.2.3-02 会员打折设置

- 功能界面：✅已实现 — 会员中心 `apps/maoxianqiu/src/views/operations/memberships/index.vue`（4 Tab：会员等级/客户会员/积分流水/折扣规则，L22-27）。
- 界面功能实现：✅ — 会员等级维护（L101-170：编码/名称/基础折扣收取比例/积分倍率/排序/启停）；折扣规则增删改（L413-479：等级/适用门店/目录类型/具体项目/折扣/优先级/启停，弹窗 L648+）；客户会员调整等级（L553-585）。
- 业务流转流程：✅ — membership_discount_rules 表 `supabase/migrations/20260810000056_membership_discount_rules.sql`（L14-33：tier/store/catalog_item/catalog_type/discount_percent/priority/is_active，具体项目与类型互斥约束；RLS membership.manage）；Billing 真实接入 `20260810000057_membership_billing_integration.sql`：invoice_items.membership_discount_percent 快照列（L15-16）+ `get_effective_membership_discount` RPC（L22-70+，优先级=具体项目>目录类型>等级默认、同维度门店>全门店）+ create_invoice `p_apply_membership_discount` 服务端权威重算快照（billing.ts L300 调用）；价格快照不可变（历史发票不受后续规则修改影响）。
- 判定：已实现
- 说明：需求报告判定"已覆盖"与代码一致；收银折扣来源 membership_discount_rules + 快照落库完整。

## 3.4.2.3-03 小票打印设置

- 功能界面：✅已实现（打印设置维护界面）— 系统设置-打印 Tab（settings/index.vue L725-754：纸型/名称/默认/启停维护）+ 打印中心 `apps/maoxianqiu/src/views/operations/print/index.vue`（打印任务列表 L25-128、新建打印任务选择模板+业务单据）+ 文档中心 `operations/documents/index.vue`（模板列表（门店/租户/系统优先级标签 L53-61）/预览/渲染/打印/历史）。
- 界面功能实现：✅ — print_settings 维护弹窗（settings/index.vue L372-419：纸型 58mm/80mm/A4、名称、默认、启停）；打印中心按 document_templates 模板执行打印。
- 业务流转流程：⚠️ — print_settings 表（`20260809000035_tenant_init_schema.sql` L98-118：paper_size/label/is_default/is_active/config jsonb 预留份数/边距/水印，每门店唯一默认）；document_templates 表（`20260810000108_document_templates.sql` L18-40：8 种文档类型、A4/80mm/58mm、Store>Tenant>System 优先级）+ `api/services/documents/index.ts` `resolveTemplate`（L27-117 三级优先级）；print_jobs 打印任务。
- 判定：不完整实现
- 说明：打印参数维护界面、模板/纸型/任务链路已落地；但 PRD §27.1.3 的"小票显示内容项开关"（是否显示客户手机号/宠物信息/操作员/医生/折扣前金额、抬头/页脚与声明等）未提供专项维护 UI——仅以 document_templates.template_json 与 print_settings.config jsonb 预留，缺少可视化配置界面。

## 3.4.2.3-04 设备接入

- 功能界面：❌未实现 — 无任何设备管理/接入页面（views 下无 devices 相关视图）。
- 界面功能实现：❌ — 无"自动搜索设备"交互。
- 业务流转流程：❌ — store_devices 表在全部迁移文件中不存在（仅 stage-01 设计文档提及）；无设备心跳/测试页/本地打印代理接口代码。
- 判定：未实现
- 说明：符合预期——映射矩阵=延后（不承诺任意设备自动识别），PRD §27.1.4 明确 Web SaaS 首版不做设备自动发现；本地打印代理/设备测试页亦未落地。

## 3.4.2.3-05 Logo 设置

- 功能界面：⚠️不完整 — 租户表含 `logo_file_id` 字段（`20260806000008_tenants.sql` L14）；R2 文件服务存在（`api/lib/r2.ts` createPresignedUploadUrl L46 / createPresignedDownloadUrl L70）；但系统设置-医院信息 Tab（settings/index.vue L568-591）**无 Logo 上传控件**。
- 界面功能实现：⚠️ — 医院名称/简称/时区/币种/语言可维护；Logo 上传/预览缺失。
- 业务流转流程：⚠️ — 文档渲染仅注入 `hospital: { name, shortName }`（`api/services/documents/base.ts` L53），渲染器（renderer.ts）无 logo 变量注入；logo_file_id 未参与打印模板渲染。
- 判定：不完整实现
- 说明：品牌信息（医院名称）与打印模板变量已实现；Logo 上传 UI 与模板 Logo 渲染未落地。

## 3.4.2.3-06 钱箱设置

- 功能界面：❌未实现 — 无钱箱设置页面。
- 界面功能实现：❌ — 无添加钱箱/结账自动弹钱箱交互。
- 业务流转流程：❌ — 收银结算无钱箱联动；无钱箱指令配置代码（store_devices 表不存在）。
- 判定：未实现
- 说明：符合预期——ADR-PRN-003 决策=支持钱箱指令配置但非首版强制，毛线球有意延后，仅保留概念层面预留（无代码）。

## 3.4.2.3-07 积分设置

- 功能界面：✅已实现（部分）— 会员中心-积分流水 Tab（memberships/index.vue L587-610）+ 会员等级积分倍率维护（tierForm.pointsMultiplier L46-54）。
- 界面功能实现：✅ — 积分流水列表（客户/变动/余额/原因/时间，声明"不可修改"L589-591）；等级积分倍率可维护。
- 业务流转流程：⚠️ — point_transactions 表（`20260806000018_operations.sql` L97-109：reason 枚举 purchase/redeem/adjust/expiry，delta<>0 约束；L359-372 仅 insert 策略、无 update/delete 策略=不可变）；adjust_points RPC（L665-784：p_reason/p_operator_id/p_idempotency_key，idempotency_records 幂等命中直接返回 L704-707，积分增减+流水写入+余额更新同事务，L746-762；revoke public + grant authenticated L783-784）；points.view 权限码（20260810000056 L70-94）。
- 判定：不完整实现
- 说明：积分账户/不可变流水/手工调整（reason+operator+审计+幂等）完整；但"消费时积分获得规则"未接入收费流程——create_invoice RPC（20260810000057）与 billing.ts 均无积分计算，membership_tiers.points_multiplier 仅存储未参与消费计算；PRD §27.1.2 的消费积分比例/充值积分/退款回退/积分抵扣上限等参数不在 SETTING_REGISTRY（settings.ts L29-35 仅 5 个 business 键）。

## 3.4.2.3-08 登录提醒

- 功能界面：✅已实现（只读查询页）— 审计与安全 `apps/maoxianqiu/src/views/system/audit/index.vue`：安全事件 Tab（严重度统计卡片 critical/warning/info L409-434、严重度/事件类型筛选 L435-467、事件列表 L469-487、详情抽屉 L519-546）。
- 界面功能实现：✅ — 安全事件列表按 tenantId 查询、严重度/类型过滤、详情查看；前端类型映射含 login_failed/permission_denied/suspicious/data_export（L194-199）。
- 业务流转流程：⚠️ — security_events 表（`20260806000018_operations.sql` L277-294：event_type 枚举 + severity 枚举 + 索引）+ 查询 API `GET /operations/security-events`（`api/routes/operations.ts` L1001-1027，权限 security.view 仅 system_admin 角色、service_role 直查）+ `apiAudit.listSecurityEvents`（api/modules/audit.ts L61-62）；**事件写入缺失**：全库无 security_events insert（无登录成功/失败/敏感操作写入逻辑，注释"仅 service_role 写入"但无对应触发器/路由）；登录提醒开关设置项不存在（SETTING_REGISTRY 无安全通知键）；短信渠道 `api/providers/sms.ts` 为通用 HTTP JSON 抽象（L20-60，配置 MESSAGING_SMS_API_URL/KEY/SIGN 后可用，未配置即 PROVIDER_NOT_CONFIGURED，不 mock 伪造投递）。
- 判定：不完整实现
- 说明：安全事件页面/表/查询 API 已落地（需求报告"页面缺失"已纠正）；但事件写入与"登录情况发送到院长手机"的触发链路未实现，短信依赖环境变量配置。

## 3.4.3.1-01 主诉项目维护

- 功能界面：✅已实现（维护界面）— 统一目录页"问诊问题库" `apps/maoxianqiu/src/views/catalog/index.vue` L434-546（分类/问题/排序/状态列表、新增弹窗 L496-520、启停 L523-530、删除 L533-546）。
- 界面功能实现：✅ — 问题库 CRUD（分类如 general/history/symptom、问题内容必填、排序、启停）。
- 业务流转流程：⚠️ — intake_questions 表（`supabase/migrations/20260806000016_catalog.sql` L125-137：tenant_id/category/question/sort_order/is_active）+ `api/routes/catalog.ts` /intake-questions CRUD（L322-492，带审计）；**医生工作台未接入**：`clinical/workbench/components/EncounterEditor.vue` L29 `QUICK_COMPLAINTS = ['精神食欲','呕吐腹泻','咳嗽喷嚏','皮肤瘙痒','排尿异常','跛行']` 仍为**硬编码**数组（L78-89 渲染按钮），不读 intake_questions。
- 判定：不完整实现
- 说明：问题库维护界面/表/API 已实现（需求报告"维护界面未完成"已部分纠正），但医生工作台主诉快捷项仍是硬编码，未切换为问题库数据源。

## 3.4.3.2-01 主观诊断设置

- 功能界面：❌未实现 — 全前端无"主观诊断/医生模板"维护页面（检索"主观/主观诊断/医生模板"无命中）。
- 界面功能实现：❌ — 无根类目/子类目增删改、无诊断内容维护。
- 业务流转流程：❌ — 无对应数据表/RPC（migrations 无 subjective_diagnosis / doctor_template 相关表）。
- 判定：未实现
- 说明：PRD §31.3"主观判断模板"（作为医生输入辅助、不自动作为正式诊断）仅停留在规划；医生工作台诊断输入辅助为硬编码（EncounterEditor.vue L26 `QUICK_DIAGNOSES`，见 3.4.3.3-01）。

## 3.4.3.3-01 疾病诊断设置（诊断字典）

- 功能界面：✅已实现（维护界面）— 统一目录页"诊断字典" `apps/maoxianqiu/src/views/catalog/index.vue` L548-663（编码/名称/分类/状态列表、新增弹窗 L611-637（编码+名称必填、分类）、启停 L640-647、删除 L650-663）。
- 界面功能实现：✅ — 诊断字典 CRUD + 关键词/分类/状态筛选。
- 业务流转流程：⚠️ — diagnosis_dict 表（`supabase/migrations/20260806000016_catalog.sql` L140-153：tenant_id/code/name/category/description/is_active，(tenant_id,code) 唯一索引）+ `api/routes/catalog.ts` /diagnosis-dict CRUD（L501-588+，带审计）；**诊疗页未接入字典**：EncounterEditor.vue L26 `QUICK_DIAGNOSES = ['胃炎','皮肤病','耳炎','猫瘟(FPV)','犬细小(CPV)','尿结石','牙周病']` 硬编码（L105-117 渲染按钮），诊断录入为"快捷项+自由文本"，不读 diagnosis_dict；多诊断/初步/确认/鉴别等结构化诊断（PRD §10.4）未在 EncounterEditor 中体现。
- 判定：不完整实现
- 说明：诊断字典维护界面/表/API 已实现（需求报告"维护界面未完成"已部分纠正），但医生工作台诊断快捷项仍是硬编码，未改用字典数据（含自由文本判定）。

## 汇总

| 需求点 | 界面 | 界面功能 | 业务流转 | 判定 |
|---|---|---|---|---|
| 3.4.1.1-01 机构信息 | ✅ | ✅ | ✅ | 已实现 |
| 3.4.1.2-01 用户角色 | ✅ | ✅ | ✅ | 已实现 |
| 3.4.1.3-01 权限管理 | ✅ | ✅ | ✅ | 已实现 |
| 3.4.1.4-01 系统用户 | ✅ | ✅ | ✅ | 已实现 |
| 3.4.2.1-01 供应商 | ✅ | ✅ | ✅ | 已实现 |
| 3.4.2.2-01 数据导入 | ✅ | ✅ | ✅ | 已实现 |
| 3.4.2.3-01 系统参数 | ✅ | ✅ | ✅ | 已实现 |
| 3.4.2.3-02 会员打折 | ✅ | ✅ | ✅ | 已实现 |
| 3.4.2.3-03 小票打印 | ✅ | ✅ | ⚠️ | 不完整实现 |
| 3.4.2.3-04 设备接入 | ❌ | ❌ | ❌ | 未实现（有意延后） |
| 3.4.2.3-05 Logo | ⚠️ | ⚠️ | ⚠️ | 不完整实现 |
| 3.4.2.3-06 钱箱 | ❌ | ❌ | ❌ | 未实现（有意延后） |
| 3.4.2.3-07 积分 | ✅ | ✅ | ⚠️ | 不完整实现 |
| 3.4.2.3-08 登录提醒 | ✅ | ✅ | ⚠️ | 不完整实现 |
| 3.4.3.1-01 主诉 | ✅ | ✅ | ⚠️ | 不完整实现 |
| 3.4.3.2-01 主观诊断 | ❌ | ❌ | ❌ | 未实现 |
| 3.4.3.3-01 疾病诊断 | ✅ | ✅ | ⚠️ | 不完整实现 |

**主要差距说明：**

1. **访问控制（3.4.1 组，4/4 已实现）**：RBAC+RLS 体系完整（角色/权限码/系统用户/机构两级维护），与需求报告"已覆盖"一致，无需返工。
2. **系统数据（3.4.2 组）**：
   - 供应商（3.4.2.1-01）、系统参数中心（3.4.2.3-01）、数据导入闭环（3.4.2.2-01）三项**需求报告标"部分覆盖/待确认"，本次核实已实现**（导入幂等已由 stage-04 收口）。
   - 小票打印（3.4.2.3-03）：打印参数维护与模板链路已实现，缺"小票显示内容项开关"专项 UI（仅 config jsonb 预留）。
   - Logo（3.4.2.3-05）：缺 Logo 上传 UI 与模板 Logo 渲染。
   - 积分（3.4.2.3-07）：流水/调整/幂等完整，缺"消费积分获得规则"（points_multiplier 未接入收费计算、积分参数不在 SETTING_REGISTRY）。
   - 登录提醒（3.4.2.3-08）：页面/表/查询 API 已实现，**security_events 无写入实现**、登录提醒触发与短信通知（依赖环境变量配置）未落地。
3. **系统设置（3.4.2.3 组）**：设备接入/钱箱为毛线球**有意延后**（映射矩阵=延后、ADR-PRN-003=非首版强制），store_devices 表未建，判定"未实现"需在手册对照时备注理由，不作为缺陷。
4. **医学数据（3.4.3 组）**：**问诊问题库与诊断字典的维护界面、数据表、CRUD API 均已实现**（位于统一目录页 catalog，非独立页面）；但医生工作台 EncounterEditor.vue 的主诉快捷项（QUICK_COMPLAINTS）与诊断快捷项（QUICK_DIAGNOSES）仍为**硬编码数组，未接入问题库/诊断字典数据源**；主观诊断模板（3.4.3.2-01）全库无实现。这是本组唯一需要继续开发的部分（接入数据源 + 实现主观判断模板）。
